# 实施计划 · CRM 客户去重系统（创建闸 + 存量批处理）

> 状态：已批准设计（`docs/crm-dedup-design.md`，brainstorming P6 产物）→ 本计划为 writing-plans 产物，含真实路径 + 完整代码 + 测试 + 每任务 commit。
> 项目根：`D:\system\CRM-ai-native`
> 测试框架：vitest（`npm test` → `node node_modules/vitest/vitest.mjs run`）

## 校准结论（基于真实源码探查）

| 项 | 真实情况 |
|---|---|
| 去重地基 | **已存在** `src/particles/dedup.js`（`confirmMerge` 软合并标记 `meta.merged_into`）+ `src/sales/accountGuard.js:20 normalizeAccountName` 可复用 |
| 创建写入口 | `src/action/seed-actions.js:76` 注册 `data-particle-create`，handler 直接调 `createParticle`——**MCP/Web/agent 三入口共用 chokepoint**，是去重 hook 最佳落点 |
| 存储层 | `src/particles/particleRepo.js`：`createParticle` 是纯 INSERT（create-only，印证造重根因）；`updateParticle(id, {patch})` 已存在可用；`meta` 列经 `ensureDedupSchema()` 保证 |
| bootstrap 豁免范式 | `seed-actions.js` 与 `particleRepo.js` 均有 `ctx.bootstrap` 豁免（demo/seed 数据不查重），直接复用 |
| 真实 agent key | `intake-router`（账户/线索建档路由，主责）/ `review-gate`（看板 UI 治理）。**无** `crm-write`/`crm-copilot` 占位居，设计文档 contract 已据此修正 |
| 测试范式 | `test/account-guard.test.js`：`beforeEach TRUNCATE crm.particles, crm.edges RESTART IDENTITY CASCADE`；直接 `import { createParticle } from '../src/particles/particleRepo.js'` |

## 设计决策（与已批准设计一致）

- **创建时查重 + 自动静默归并（方案 B）**：高置信命中 → 不新建，字段级并入已有账户（根绝重复）。
- **复合高置信门槛**：① 归一化精确相等 + 行业相同（创建闸主判据）；或 ② 模糊相似≥0.9 + 联系人/电话重叠（存量批处理时可用，因创建时尚未有联系人）。
- **低置信降级**：仅模糊相似（≥0.6 且非精确）→ 仍创建，但标 `payload.possible_duplicate_of`，前端高亮提示（不静默归并）。
- **零信任**：归并走软合并（`confirmMerge` 标记 `merged_into`），**不物理删除**；存量批处理高置信自动归并、低置信仅出清单不自动动。

---

## 任务 1 · 去重检测服务（算法核心）

**文件**：`src/particles/dedup.js`（在现有文件追加导出，不新建）
**agent**：`intake-router` ｜ **skills**：`[data-particle-read]` ｜ **memory**：`[intake-router]`
**success**：传入与 `fa21164f` 同名的 payload → 返回 `decision:'MERGE'` 且 `candidateId` 含 `fa21164f`；全新客户 → `decision:'CREATE'`

在文件末尾追加：

```js
import { normalizeAccountName } from '../sales/accountGuard.js';
import { getParticle } from './particleRepo.js';

const FUZZY_MERGE = 0.9;   // 高置信（存量批处理，需配合联系人重叠）
const FUZZY_PROMPT = 0.6;  // 低置信阈值（仅提示，不静默归并）

// 编辑距离（DP）
function levenshtein(a, b) {
  const m = a.length, n = b.length;
  const dp = Array.from({ length: m + 1 }, (_, i) => [i, ...Array(n).fill(0)]);
  for (let j = 0; j <= n; j++) dp[0][j] = j;
  for (let i = 1; i <= m; i++)
    for (let j = 1; j <= n; j++)
      dp[i][j] = a[i - 1] === b[j - 1] ? dp[i - 1][j - 1]
        : 1 + Math.min(dp[i - 1][j - 1], dp[i - 1][j], dp[i][j - 1]);
  return dp[m][n];
}
function similarity(a, b) {
  const s1 = normalizeAccountName(a), s2 = normalizeAccountName(b);
  if (!s1 || !s2) return 0;
  if (s1 === s2) return 1;
  const max = Math.max(s1.length, s2.length) || 1;
  return 1 - levenshtein(s1, s2) / max;
}

// 创建闸主判据：归一化精确相等 + 行业相同（或无行业约束）→ MERGE
//   composite ②（模糊+联系人）在 backfill 场景经 hasContactOverlap 触发
export async function detectAccountDuplicate(name, industry, tenantId = 'system') {
  const nn = normalizeAccountName(name);
  if (!nn) return { decision: 'CREATE', candidateId: null };
  const rows = (await query(
    `SELECT id, payload FROM crm.particles
     WHERE tenant_id=$1 AND type='CRM_ACCOUNT'
       AND (meta->>'merged_into') IS NULL`,
    [tenantId]
  )).rows;
  let best = null, bestSim = 0;
  for (const r of rows) {
    const p = r.payload || {};
    if (p.name && normalizeAccountName(p.name) === nn) {
      // 归一精确相等：行业相同或无行业约束 → 高置信 MERGE
      if (!industry || !p.industry || p.industry === industry) {
        return { decision: 'MERGE', candidateId: r.id, reason: 'exact_name+industry' };
      }
      best = best || { id: r.id, sim: 1, p }; // 同名但行业不同：不直接判 MERGE，留给模糊兜底
    }
    const sim = similarity(name, p.name || '');
    if (sim > bestSim) { bestSim = sim; best = { id: r.id, sim, p }; }
  }
  // 低置信：仅模糊相似（≥PROMPT 且非精确）→ PROMPT（降级提示，不静默归并）
  if (best && bestSim >= FUZZY_PROMPT) {
    return { decision: 'PROMPT', candidateId: best.id, similarity: Number(bestSim.toFixed(3)) };
  }
  return { decision: 'CREATE', candidateId: null };
}

// 存量批处理用：模糊≥0.9 + 任意联系人/电话重叠 → MERGE
export async function detectAccountDuplicateDeep(name, industry, tenantId = 'system') {
  const base = await detectAccountDuplicate(name, industry, tenantId);
  if (base.decision !== 'PROMPT' || !base.candidateId) return base;
  const cand = await getParticle(base.candidateId);
  const candPayload = cand?.payload || {};
  const overlap = hasContactOverlap(industry, candPayload);
  if (base.similarity >= FUZZY_MERGE && overlap) {
    return { decision: 'MERGE', candidateId: base.candidateId, reason: 'fuzzy+contact' };
  }
  return base; // 仍 PROMPT
}

function hasContactOverlap(incomingContact, candPayload) {
  // incomingContact: 创建时可为空（用于 PROMPT 场景）；存量为 {phone, contacts:[...]}
  const aPhones = collectPhones(incomingContact);
  const bPhones = collectPhones(candPayload);
  if (aPhones.size && bPhones.size) {
    for (const p of aPhones) if (bPhones.has(p)) return true;
  }
  return false;
}
function collectPhones(obj) {
  const s = new Set();
  if (!obj) return s;
  if (obj.phone) s.add(String(obj.phone));
  for (const c of (obj.contacts || [])) if (c?.phone) s.add(String(c.phone));
  return s;
}
```

**测试**：`test/dedup.test.js`（见任务 5 统一测试文件，任务 1 用例在第 1 段）

**commit**：`git add src/particles/dedup.js && git commit -m "feat(dedup): 账户查重检测服务 detectAccountDuplicate（+deep 联系人重叠版）"`

---

## 任务 2 · 创建闸集成（写前置 hook）

**文件**：`src/action/seed-actions.js`（仅改 `data-particle-create` handler，第 92 行起）
**agent**：`intake-router` ｜ **skills**：`[data-particle-create, data-particle-read]` ｜ **memory**：`[intake-router]`
**success**：以 `9ac7d5e3` 同名 payload 再调创建 → 返回 `merged:true` 且不新建第 2 个粒子；全新客户 → 正常 CREATE

修改 `data-particle-create` 的 `handler`（第 92 行）：

```js
// 顶部 import 区新增：
import {
  detectAccountDuplicate, detectAccountDuplicateDeep,
  mergeIntoExisting, mergeTwoAccounts,
} from '../particles/dedup.js';

// handler 改为：
handler: async ({ type, payload }, ctx) => {
  // 【去重创建闸】CRM_ACCOUNT 前置查重（bootstrap/seed 豁免，等同 accountGuard.accountGuard 范式）
  if (type === 'CRM_ACCOUNT' && ctx.bootstrap !== true) {
    const dup = await detectAccountDuplicate(payload?.name, payload?.industry, ctx.tenantId)
      .catch(() => ({ decision: 'CREATE' }));
    if (dup?.decision === 'MERGE' && dup.candidateId) {
      // 静默归并：字段级并入已有账户，不新建（根绝重复）
      const merged = await mergeIntoExisting(dup.candidateId, payload, ctx.tenantId, ctx.actor);
      return { ok: true, merged: true, particle: merged, decision_basis: 'AUTO_MERGE_DUPLICATE' };
    }
    if (dup?.decision === 'PROMPT' && dup.candidateId) {
      // 低置信：仍创建，标 possible_duplicate_of 供前端提示（不静默归并）
      payload = { ...payload, possible_duplicate_of: dup.candidateId };
    }
  }
  return createParticle(type, payload, {
    tenantId: ctx.tenantId,
    actor: ctx.actor,
    enforceNamedOwner: ctx.enforceNamedOwner === true,
    requireDecisionId: ctx.decision_id,
    accountGuard: type === 'CRM_DEAL' && ctx.bootstrap !== true,
  });
},
```

> 注意：handler 末尾原本是 `});`（第 105 行结束 `registerAction`）。新 handler 在函数体内先查重再 createParticle，结构不变。

**commit**：`git add src/action/seed-actions.js && git commit -m "feat(dedup): data-particle-create 接入创建前查重闸（静默归并/低置信标记）"`

---

## 任务 3 · 归并执行器（字段级 merge + 软合并）

**文件**：`src/particles/dedup.js`（追加）+ 复用既有 `confirmMerge`
**agent**：`intake-router` ｜ **skills**：`[data-particle-create, data-particle-read]` ｜ **memory**：`[intake-router]`
**success**：`mergeIntoExisting` 后主账户含副账户 contacts/决策链/Deal 关联；`mergeTwoAccounts` 后副账户 `meta.merged_into` 指向主

在 `dedup.js` 追加：

```js
import { getParticle, updateParticle, queryWrite } from './particleRepo.js';
import { recordAudit } from '../action/auditHook.js';

// 字段级并入：非空覆盖、数组追加去重、对象深合并
function mergePayloads(base, inc) {
  const out = { ...base };
  for (const [k, v] of Object.entries(inc || {})) {
    if (v === undefined || v === null) continue;
    if (k === 'possible_duplicate_of' || k === 'merged_into') continue; // 不回写查重标记
    if (Array.isArray(v) && Array.isArray(out[k])) out[k] = [...new Set([...out[k], ...v])];
    else if (v && typeof v === 'object' && out[k] && typeof out[k] === 'object') out[k] = { ...out[k], ...v };
    else out[k] = v;
  }
  return out;
}

// 静默归并（创建闸用）：不新建粒子，把 incoming 并入已有 candidate
export async function mergeIntoExisting(candidateId, incomingPayload, tenantId = 'system', actor = null) {
  const cur = await getParticle(candidateId);
  if (!cur) return null;
  const merged = mergePayloads(cur.payload, incomingPayload);
  const p = await updateParticle(candidateId, { patch: merged, tenantId });
  await recordAudit({
    target_particle_type: 'CRM_ACCOUNT', source: 'dedup', action: 'auto_merge',
    actor: actor || 'system', decision_id: null,
    payload: { target: candidateId, merged_keys: Object.keys(incomingPayload || {}) },
  }).catch(() => {});
  return p;
}

// 存量双账户归并：边迁移 + 字段并入 + 软合并标记（不物理删）
export async function mergeTwoAccounts(primaryId, secondaryId, tenantId = 'system') {
  // 1) 迁移受控边：source/target = secondary → primary
  await queryWrite(`UPDATE crm.edges SET source_id=$1 WHERE source_id=$2 AND tenant_id=$3`,
    [primaryId, secondaryId, tenantId]).catch(() => {});
  await queryWrite(`UPDATE crm.edges SET target_id=$1 WHERE target_id=$2 AND tenant_id=$3`,
    [primaryId, secondaryId, tenantId]).catch(() => {});
  // 2) 字段级并入
  const sec = await getParticle(secondaryId);
  if (sec) await mergeIntoExisting(primaryId, sec.payload, tenantId);
  // 3) 软合并标记（复用既有 confirmMerge，不物理删）
  await confirmMerge(secondaryId, primaryId);
  return { ok: true, primaryId, secondaryId, decision_basis: 'BACKFILL_MERGE' };
}
```

**commit**：`git add src/particles/dedup.js && git commit -m "feat(dedup): 归并执行器 mergeIntoExisting/mergeTwoAccounts（字段级+边迁移+软合并）"`

---

## 任务 4 · 存量批处理脚本

**文件**：`scripts/dedup-backfill.mjs`（新建）
**agent**：`intake-router` ｜ **skills**：`[data-particle-read, data-particle-create]` ｜ **memory**：`[intake-router]`
**success**：运行后 `9ac7d5e3` 被归并进 `fa21164f`，全库无新增重复；低置信对生成 `dedup-report.json` 而非自动动

```js
#!/usr/bin/env node
// scripts/dedup-backfill.mjs — 存量账户疑似重复扫描与归并（高置信自动，低置信出清单）
import { query, queryWrite } from '../src/db.js';
import {
  detectAccountDuplicateDeep, mergeTwoAccounts,
} from '../src/particles/dedup.js';

const TENANT = process.env.TENANT || 'system';
const DRY = process.argv.includes('--dry');

const accounts = (await query(
  `SELECT id, payload, meta FROM crm.particles
   WHERE tenant_id=$1 AND type='CRM_ACCOUNT' AND (meta->>'merged_into') IS NULL`,
  [TENANT]
)).rows;

const report = { autoMerged: [], lowConfidence: [], errors: [] };
for (let i = 0; i < accounts.length; i++) {
  for (let j = i + 1; j < accounts.length; j++) {
    const a = accounts[i], b = accounts[j];
    const name = a.payload?.name || b.payload?.name;
    const industry = a.payload?.industry || b.payload?.industry;
    try {
      const r = await detectAccountDuplicateDeep(name, industry, TENANT);
      if (r.decision === 'MERGE' && r.candidateId) {
        const primary = r.candidateId === a.id ? a.id : b.id;
        const secondary = primary === a.id ? b.id : a.id;
        if (DRY) { report.autoMerged.push({ primary, secondary, reason: r.reason, dry: true }); continue; }
        const res = await mergeTwoAccounts(primary, secondary, TENANT);
        report.autoMerged.push({ ...res, reason: r.reason });
      } else if (r.decision === 'PROMPT') {
        report.lowConfidence.push({ a: a.id, b: b.id, similarity: r.similarity });
      }
    } catch (e) {
      report.errors.push({ a: a.id, b: b.id, error: e.message });
    }
  }
}
console.log(JSON.stringify(report, null, 2));
if (!DRY) await queryWrite(`INSERT INTO crm.dedup_audit (tenant_id, report) VALUES ($1,$2)`,
  [TENANT, JSON.stringify(report)]).catch(() => {});
```

> ⚠️ 先跑 `--dry` 看报告再正式执行。当前库里 `9ac7d5e3`（无 industry 但同名归一=`m涂料科技有限公司`）会被精确匹配归并进 `fa21164f`，空壳 Deal `6485b08d` 也建议一并处理（见任务 5 测试说明）。

**commit**：`git add scripts/dedup-backfill.mjs && git commit -m "feat(dedup): 存量账户批处理脚本（高置信自动归并/低置信出清单）"`

---

## 任务 5 · 前端低置信提示 + 测试 + 审计

**文件**：
- `src/web/account-360.html`（账户详情页：读 `payload.possible_duplicate_of` 高亮）
- `src/web/named-account-manage.html`（指名客户管理：重复对清单入口）
- `test/dedup.test.js`（新建，vitest）
**agent**：`review-gate` ｜ **skills**：`[data-particle-read]` ｜ **memory**：`[review-gate]`
**success**：`possible_duplicate_of` 客户在详情/看板高亮；`dedup.test.js` 全绿

### 5a. 前端提示（account-360.html 增加 banner）
在账户详情渲染逻辑处（读 `particle.payload` 后）插入：

```js
const dupOf = p.payload?.possible_duplicate_of;
if (dupOf) {
  banner.innerHTML = `⚠️ 疑似重复客户（低置信）：本客户与 <a href="/account-360.html?id=${dupOf}">${dupOf}</a> 名称相似，请人工核对。`;
  banner.style.display = 'block';
}
```

### 5b. 测试 `test/dedup.test.js`

```js
import { describe, it, expect, beforeEach } from 'vitest';
import { query } from '../src/db.js';
import { createParticle } from '../src/particles/particleRepo.js';
import {
  detectAccountDuplicate, mergeIntoExisting, mergeTwoAccounts,
} from '../src/particles/dedup.js';
import { actionExecutor } from '../src/action/executor.js';

beforeEach(async () => {
  await query('TRUNCATE crm.particles, crm.edges RESTART IDENTITY CASCADE');
});

describe('detectAccountDuplicate（纯函数/集成）', () => {
  it('归一精确同名+行业相同 → MERGE', async () => {
    const a = await createParticle('CRM_ACCOUNT', { name: 'M 涂料科技有限公司', industry: '涂料/化工', named_owner: 'alice' });
    const r = await detectAccountDuplicate('m涂料科技有限公司 ', '涂料/化工', 'system');
    expect(r.decision).toBe('MERGE');
    expect(r.candidateId).toBe(a.id);
  });
  it('不同名 → CREATE', async () => {
    await createParticle('CRM_ACCOUNT', { name: 'A 公司', industry: 'x', named_owner: 'alice' });
    const r = await detectAccountDuplicate('B 公司', 'y', 'system');
    expect(r.decision).toBe('CREATE');
  });
  it('仅模糊相似（非精确）→ PROMPT', async () => {
    await createParticle('CRM_ACCOUNT', { name: '深圳兴达包装', industry: '包装', named_owner: 'alice' });
    const r = await detectAccountDuplicate('深圳兴达包装材料', '包装', 'system');
    expect(r.decision).toBe('PROMPT');
    expect(r.candidateId).toBeTruthy();
  });
});

describe('mergeIntoExisting（静默归并，不新建）', () => {
  it('字段级并入且返回已有 id', async () => {
    const a = await createParticle('CRM_ACCOUNT', { name: 'M 涂料科技', industry: '涂料/化工', contacts: [{ name: '苏研发' }], named_owner: 'alice' });
    const merged = await mergeIntoExisting(a.id, { contacts: [{ name: '吕生产' }], phone: '123' }, 'system');
    expect(merged.id).toBe(a.id);
    expect(merged.payload.phone).toBe('123');
    expect(merged.payload.contacts.length).toBe(2);
  });
});

describe('mergeTwoAccounts（存量双账户软合并）', () => {
  it('边迁移 + merged_into 标记', async () => {
    const primary = await createParticle('CRM_ACCOUNT', { name: 'M 涂料科技', named_owner: 'alice' });
    const sec = await createParticle('CRM_ACCOUNT', { name: 'M涂料科技有限公司', named_owner: 'alice' });
    const contact = await createParticle('CRM_CONTACT', { name: '苏研发' }, { tenantId: 'system' });
    await queryWrite(
      `INSERT INTO crm.edges (tenant_id, source_type, source_id, edge_type, target_type, target_id, cardinality, meta)
       VALUES ('system','CRM_ACCOUNT',$1,'has_contact','CRM_CONTACT',$2,'many','{}')`,
      [sec.id, contact.id]);
    const res = await mergeTwoAccounts(primary.id, sec.id, 'system');
    expect(res.ok).toBe(true);
    const secAfter = (await query(`SELECT meta FROM crm.particles WHERE id=$1`, [sec.id])).rows[0];
    expect(secAfter.meta.merged_into).toBe(primary.id);
    const e = (await query(`SELECT source_id FROM crm.edges WHERE target_id=$1`, [contact.id])).rows[0];
    expect(e.source_id).toBe(primary.id); // 边已迁移
  });
});

describe('创建闸集成（data-particle-create handler）', () => {
  it('同名再创建 → 静默归并不新建', async () => {
    const ctx = { tenantId: 'system', actor: 'alice', bootstrap: false, decision_id: null };
    const r1 = await actionExecutor.dispatch('data-particle-create',
      { type: 'CRM_ACCOUNT', payload: { name: 'M 涂料科技', industry: '涂料/化工' } }, ctx);
    // 第二调用（模拟用户再建同名）—— bootstrap=false 触发查重
    const r2 = await actionExecutor.dispatch('data-particle-create',
      { type: 'CRM_ACCOUNT', payload: { name: 'm涂料科技有限公司', industry: '涂料/化工' } }, ctx);
    expect(r2.data?.merged).toBe(true);
    expect(r2.data.particle.id).toBe(r1.data.id); // 同一账户，无重复
    const cnt = (await query(`SELECT COUNT(*) FROM crm.particles WHERE type='CRM_ACCOUNT'`)).rows[0].count;
    expect(Number(cnt)).toBe(1);
  });
  it('bootstrap（seed）豁免查重', async () => {
    const ctx = { tenantId: 'system', actor: 'system', bootstrap: true, decision_id: null };
    await actionExecutor.dispatch('data-particle-create',
      { type: 'CRM_ACCOUNT', payload: { name: '演示A', named_owner: 'system' } }, ctx);
    const r2 = await actionExecutor.dispatch('data-particle-create',
      { type: 'CRM_ACCOUNT', payload: { name: '演示A', named_owner: 'system' } }, ctx);
    expect(r2.data?.merged).toBeFalsy(); // seed 允许重复（历史演示数据）
  });
});
```

> 运行：`node node_modules/vitest/vitest.mjs run test/dedup.test.js`
> 关于空壳 Deal `6485b08d`：它不是账户，不在本次账户去重范围；建议单独 `UPDATE crm.particles SET state='MERGED' WHERE id='6485b08d-...'`（或经 `confirmMerge`）软弃用，避免 Web 空壳商机干扰。本测试不覆盖 Deal，仅账户。

**commit**：`git add src/web/account-360.html src/web/named-account-manage.html test/dedup.test.js && git commit -m "feat(dedup): 前端低置信提示 + dedup 测试套件（vitest）"`

---

## 执行顺序与验证

1. 依序执行任务 1→5，每任务一 commit（隔离变更、便于回滚）。
2. 每任务后跑 `npm test`（或单文件）确认不回归。
3. 任务 4 先 `node scripts/dedup-backfill.mjs --dry` 审阅报告，确认 `9ac7d5e3→fa21164f` 在 autoMerged 列表，再正式运行。
4. 全量回归：`npm test`（基线约 386 例，PG 不稳时单次红不得直判回归）。

## 护栏（误并缓解）

- ✅ `confirmMerge` 软合并（`meta.merged_into`）→ 误并可定位、可逆。
- ✅ 低置信**绝不**自动归并，仅 `possible_duplicate_of` 提示 / 批处理清单。
- ✅ `merged_into` 在 `detectAccountDuplicate` 查询中排除（`(meta->>'merged_into') IS NULL`）→ 已合并者不再参与后续匹配。
- ✅ 零信任：全程无物理删除，符合 CRM-ai-native「绝对禁删」铁律。
