# 设计文档比对审计：09-15 晚间四份设计 → 唯一最终设计

> 目的：核对 2026-09-15 晚间的四份设计文档与唯一最终设计之间的**合并保真度**，并交叉核验「设计承诺 ↔ 代码实现」的偏差。
> 取证方式：全部结论基于 `file:line` 与 grep 命中数，双向核对（设计侧 ↔ 实现侧）。
> 日期：2026-09-16

---

## §0 结论速览

| # | 维度 | 结论 |
| - | ---- | ---- |
| 1 | 任务编号映射 | ✅ **10/10 + 10/10 逐字一致**（线A T01–T10、线B T11–T20） |
| 2 | 口径修正记录 | ⚠️ **丢失 3 条**；且**未记录 1 处真实范围反转**（深度合并 → 红线） |
| 3 | 内容存活 | ❌ **Rox 借鉴清单 5/6 项、Rox 红线 2/4 条、对外叙事三句、1 条对外表述红线 → 无归属** |
| 4 | 分段（S）语义 | ⚠️ **三套并存且互不兼容**；废弃 banner 只映射 T 编号，**未提示 S 已重排** |
| 5 | DDL 保真 | ⚠️ 4 表一致；**signal 差 5 列、standing_grant 差 2 列**；advice_record **未进 §8** |
| 6 | 引用完整性 | ❌ **4 处断链**（2 处文件不存在、1 处锚定废弃文档、1 处内部交叉引用错节） |
| 7 | 设计承诺 ↔ 实现 | ❌ **线A 补齐 8 项中 5 项未实现**；**线A 全链路在 `src/` 下无生产触发点** |
| 8 | 旁证文档 | ⚠️ 夜批审计报告含**失效红色断言**（false negative），未随交付更新 |

**一句话结论**：**编号映射做到了逐字保真，但内容层面发生了系统性收窄**——最终设计把范围锁定在"线A 共生同步 + 线B 主动运行时"两条工程线，Rox 对标文档里的**产品/定价/技能/富化/反馈**五类借鉴项与两条红线**未被任何权威文档承接**；同时**线A 已交付的模块与表在 `src/` 下没有任何生产触发点**，属项目自身最忌讳的「清单在但挂载方没来」同源缺陷。

---

## §1 文档拓扑：4 废弃 → 1 最终

| 文档 | 行数 | 状态 | 使命 |
| ---- | ---- | ---- | ---- |
| `2026-09-15-proactive-runtime-design.md` | 696 | ⛔ SUPERSEDED | 线B 主动运行时（原 T1–T10） |
| `2026-09-15-crm-coexistence-sync-design.md` | 621 | ⛔ SUPERSEDED | 线A 共生同步（原 T1–T10） |
| `2026-09-15-rox-benchmark-differentiation-analysis.md` | 341 | ⛔ SUPERSEDED | Rox 单点对标 |
| `2026-09-15-attio-lightfield-rox-three-way-comparison.md` | 311 | ⛔ SUPERSEDED | 三方对照 |
| `2026-09-15-final-design-coexistence-and-proactive.md` | 1838 | ✅ **唯一有效** | 合并最终设计 v1.0 |
| `2026-09-15-mcp-oauth-design.md` | — | ✅ 独立存活 | MCP OAuth（**未**被合并，另附 verification） |

- 四份废弃文档合计 **1969 行** → 最终 **1838 行**（压缩比 ~93%），差额即去重与收窄量。
- 最终设计第 8–11 行**反向列出这 4 份废弃文档**为设计输入；废弃文档顶部各有 banner 指向最终设计。
- **注意**：废弃 banner 自述「仅保留过程证据、DDL 草案与一手资料锚点，**不得作为实施、评审或对外表述的依据**」——这意味着**凡只存在于废弃文档的结论，当前处于"事实无家可归"状态**（见 §4）。

---

## §2 编号映射保真度：✅ 逐字一致

文档内声明的映射规则：`A-Tn → T0n`（线A）；`B-Tn → T1n`（线B）。

### 2.1 线A（共生同步）——旧 T1–T10 vs 新 T01–T10

| 旧 | 新 | 标题 | 一致 |
| -- | -- | ---- | ---- |
| T1 | T01 | 同步内核 + 字段映射层 | ✅ 逐字 |
| T2 | T02 | 纷享销客适配器 | ✅ |
| T3 | T03 | 外部引用映射与实体对齐 | ✅ |
| T4 | T04 | 单向回写通道 | ✅ |
| T5 | T05 | 同步信任分级 | ✅ |
| T6 | T06 | 事件订阅接入 | ✅ |
| T7 | T07 | 同步可观测 | ✅ |
| T8 | T08 | 产品与价格表同步（报价基线来源） | ✅ |
| T9 | T09 | 同步记录与拓客候选去重 | ✅ |
| T10 | T10 | 接入与映射变更评审闸门（HITL 落点） | ✅ |

### 2.2 线B（主动运行时）——旧 T1–T10 vs 新 T11–T20

| 旧 | 新 | 标题 | 一致 |
| -- | -- | ---- | ---- |
| T1 | T11 | 信号统一收口 | ✅ 逐字 |
| T2 | T12 | 感知三源触发器 | ✅ |
| T3 | T13 | 投递抽象层与四渠道 provider | ✅ |
| T4 | T14 | 信号进入工作台与首页信号卡 | ✅ |
| T5 | T15 | 报价与阶段推进的时间型信号 | ✅ |
| T6 | T16 | 主动拓客信号 | ✅ |
| T7 | T17 | L3 智能体主动研究：产出建议卡 | ✅ |
| T8 | T18 | 建议卡采纳回路与 J2 回写 | ✅ |
| T9 | T19 | 常驻授权凭证与 T1 自动执行 | ✅ |
| T10 | T20 | 链路观测与信任校准 | ✅ |

### 2.3 净新增

| 新增 | 来源 | 说明 |
| ---- | ---- | ---- |
| **T21** | 两份旧文档均无 | 分级授权对象化（A 轴 D1–D4 + E1/E2），即最终设计 §0.4 追加的「先修授权依据」 |

**契约块实测**：`contract-yaml` fence 共 **22** 处 → 行号 `1054/1068/.../1341`（= T01–T21，**21 块**）+ `1475`（§16 验收标准内的示例块）。**编号映射无遗漏、无重号。**

---

## §3 口径修正保真度：⚠️ 丢失 3 条

最终 §附录B「修正记录（本设计推翻的旧判断）」含 **11 条**。

旧文档附录 B 合计 9 条（crm-coexistence 5 条 + three-way 4 条；rox-benchmark 无附录 B）。

| 旧文档条目 | 是否进最终附录B | 备注 |
| ---------- | -------------- | ---- |
| 客户「多数没有成熟 CRM」 | ✅ | 且升级为 0.1「最重要的修改」 |
| 「事实发生地」是 P0 | ✅ | 降为补充轨 |
| 「数据进来」要建 CSV | ✅ | 改为 API 优先 |
| 「同步机制我方不缺，缺的是对象」 | ✅ | 并入「缺同步机制 🔴 推翻」 |
| Rox 三层同步最难的是冲突消解 | ❌ **丢失** | 结论仍成立（我方已有同构 CAS） |
| 「我方缺外部接入器」 | ✅ | |
| Rox 用 Agent Action 计价 | ❌ **丢失** | 见 §4 D1（连带整项被弃） |
| Attio/Lightfield 都是「取代」 | ❌ **丢失** | 内容仍在最终 §1.2 定位象限 |
| `landing.html`「销售易」误读 | ✅ | |

### 3.1 ⚠️ 未记录的**范围反转**

| 项 | 旧 crm-coexistence §7.2 | 最终设计 | 是否记入附录B |
| -- | ---------------------- | -------- | ------------ |
| 深度合并 / Batch 通道接口预留 | **S5 交付段**（「增量字段更新不误伤同层其它字段」） | **§15.2 #6 红线：完整双向同步…不做** | ❌ **未记录** |

> 这是一处**真实的交付范围反转**（原计划交付 → 终版明确不做），结论正确（最终设计内部自洽），但**修正记录里没有它**，属追溯断点。

---

## §4 内容存活审计：❌ 实质丢失 4 类

### D1（最严重）Rox 借鉴清单 **5/6 项无家可归**

旧 `rox-benchmark-differentiation-analysis.md` §5「借鉴清单（按 ROI 排序）」6 项，逐一核对最终设计与 09-16 竞品学习清单：

| 旧项 | 优先级 | 最终设计命中 | 竞品学习清单命中 | 归属 |
| ---- | ------ | ------------ | --------------- | ---- |
| Agent Action 计价单元（业务动作计价表） | **P0** | `计价单元` 0 / `计价` 0 | 0 | ❌ **无** |
| 「为什么」一键产品化 | P1 | `provenance` 仅 1 处 | 0 | 🟡 弱 |
| 反馈采集前端化（三态反馈） | P1 | `反馈采集` 0 / `三态反馈` 0 | 0 | ❌ **无** |
| Skills 自助化（受控版） | P1 | `Skills 自助` 0 | 0 | ❌ **无** |
| 富化供应链补齐 | P2 | `富化供应链` 0 / `waterfall` 0 | 0 | ❌ **无** |
| 事件触发矩阵扩展 | P2 | 由 **T12 感知三源触发器** 承接 | — | ✅ |

**其中 P0 项（Agent Action 计价单元）在最终设计中仅以一条 URL 存在**：`§附录A.2` 第 `Agent Action 计量：$100/月=10k actions…`（第 1787 行）——即**从"待办建议"降级为"参考资料"**。

### D2 Rox 对标文档的 **2 条红线丢失**

旧 `rox-benchmark` §6 四条红线 vs 最终 §15 九条：

| 旧红线 | 最终 §15 命中 |
| ------ | ------------ |
| 全自动对外触达 | ✅ 15.3 #8（部分并入「不学 always-on」） |
| warehouse-native 单租户 in-VPC 深部署 | ❌ `深部署` 0 / `in-VPC` 0 |
| 用 Tether 类自研协议替代 MCP | ❌ `Tether` 0 |
| 「取消手动录入 / 替代 CRM」 | ✅ 15.1 #1 |

### D3 三方对照 §9「可引用叙事三句」**全部丢失**

原文标注「（三句，**供对外材料**）」：对客户 / 对行业评审 / 对投资人。核对最终设计：

| 关键短语 | 最终设计命中 |
| -------- | ------------ |
| 「不需要搬」 | 0 |
| 「自建记录系统 + 上下文共生层」 | 0 |
| 「用纪律换可验证，而不是用规模换概率」 | 0 |

> **影响面**：用户在同期准备 AI OPC 申报与参赛材料，此类对外口径属可直接复用资产，现仅存于标注"不得作为对外表述依据"的废弃文档中。

### D4 三方对照 §8 #4「对外表述红线」丢失

旧条目：*不学"数据可随意搬走、零 egress 费"的对外承诺；我方对应能力是租户隔离，表述应是"**数据不出租户边界**"*。

| 关键短语 | 最终设计命中 |
| -------- | ------------ |
| `egress` | 0 |
| `租户边界` | 0 |

> 最终 §12 保留了「租户隔离」**技术铁律**，但**对外表述约束**消失——二者不可互相替代（前者是机制，后者是口径）。

### ✅ D5 存活良好的部分（对照回写）

- 定位反转（§0.1）✅ 被强化为"最重要的一条修改"
- §1.2 定位象限（Attio 搬迁 / Lightfield 低摩擦搬迁 / Rox 借家 / 我方长期共生）✅
- §1.3「从 Rox 借什么/不借什么」10 行表 ✅（覆盖三层同步/乐观并发/静态标记/互补叙事/终局不借/投递优先/常驻监控/限档借 autopilot/治理轴差异）
- 附录 A.3 grep 零命中证据 ✅（且**今日复测仍成立**：`fxiaoke` 现已有实现，属实现推进而非文档失真）
- Rox 官方免责声明（"须人工复核"）作为 HITL 外部背书 ✅
- §11.3 T0–T3 授权档位 + §11.4 熔断/降级/撤回 ✅（完整承接「已定 T1 内部字段」）

---

## §5 分段（S）语义冲突：⚠️ 高风险

**三套 S 编号并存，语义互不兼容：**

| 文档 | S 编号范围 | 归属 | 示例冲突 |
| ---- | ---------- | ---- | -------- |
| 旧 crm-coexistence §7.2 | S1–S5 | 仅线A | 其 **S5** = 「深度合并 + Batch 通道」 |
| 旧 proactive §17.1 | S1–S5 | 仅线B | 其 **S2** = 三源触发器 + 报价节律 + 拓客信号 |
| 最终 §14.3 | **S1–S7** | 两线交织 | 其 **S5** = 线B 上述三项；其 **S2/S3/S4** = 线A |

**逐条差异（5 + 7 = 12 个任务换段）：**

| 任务 | 旧段 | 新段 |
| ---- | ---- | ---- |
| T04 单向回写 | 旧A-S3 | 新 **S4** |
| T06 事件订阅 | 旧A-S4 | 新 **S3** |
| T07 同步可观测 | 旧A-S4 | 新 **S3** |
| T08 报价基线 | 旧A-S2 | 新 **S3** |
| T09 拓客去重 | 旧A-S2 | 新 **S3** |
| T12 三源触发器 | 旧B-S2 | 新 **S5** |
| T15 时间型信号 | 旧B-S2 | 新 **S5** |
| T16 拓客信号 | 旧B-S2 | 新 **S5** |
| T17 主动研究 | 旧B-S3 | 新 **S5** |
| T18 采纳回路 | 旧B-S3 | 新 **S5** |
| T19 常驻授权 | 旧B-S4 | 新 **S6** |
| T20 观测校准 | 旧B-S5 | 新 **S7** |

> ⚠️ **残余风险**：废弃 banner 仅给出 **T 编号映射**，**未提示 S 编号已整体重排**。任何依据旧文档定位「T5 属 S2」的人，会落到新设计的 **S5**（而非 S2）——错位 3 段。
> ✅ **正面验证**：09-16 的 7 份实施计划（`*-s1-delivery` … `*-s7-observability`）**采用最终设计的 S 语义**，实施侧未踩此坑。

---

## §6 DDL 漂移：设计方案 ↔ 实际 schema

| 表 | 设计列数（最终§8） | 实现列数（`db/schema.sql`） | 判定 |
| -- | ----------------- | --------------------------- | ---- |
| `external_ref` | 14（§8.1，:1083） | 14 | ✅ 一致 |
| `sync_cursor` | 13（§8.2，:1106） | 13 | ✅ 一致 |
| `signal_delivery` | 12（§8.4，:1059） | 12 | ✅ 一致 |
| `grant_execution` | 13（§8.6，:1196） | 13 | ✅ 一致 |
| **`signal`** | **18**（§8.3，:1028） | **17** | ❌ **差 5 处** |
| **`standing_grant`** | **19**（§8.5，:1169） | **21** | ❌ **+2 列** |
| **`advice_record`** | **不在 §8**（:1131） | **18** | ❌ **未进设计** |

### 6.1 `crm.signal` 逐列差异

- 设计有 / 实现无：**`decision_id`**、**`action_ref`**、**`closed_reason`**
- 实现有 / 设计无：**`l2c_stage`**、**`acted_at`**
- `status` 取值域：设计未明示；实现扩为 `open | acked | closed | acted`
- 去重索引：设计未给谓词；实现为 `WHERE dedup_key IS NOT NULL AND status IN ('open','acked')`（**2026-09-16 修真库缺陷后收窄**，属实现侧改进）

> ⚠️ `decision_id` / `action_ref` 的缺失值得注意：设计本意是**信号可携带决策凭证与动作引用**（血缘），实现未落。而 §12 铁律又要求「产生无 decision_id 的粒子写入 = 违反」——此处信号不落 decision_id 属另一维度，但**设计意图未被实现或未被显式否决**。

### 6.2 `crm.standing_grant` 差异

实现 +`paused_at` / +`paused_reason`（S7 T20-3 为「降级事件可追溯」新增，迁移 `db/migration-standing-grant-paused.sql`）。**迁移已落，设计 §8.5 未回填**。

### 6.3 `crm.advice_record` 未进 §8

- 最终 §12 仍称「**6 张新表**」；§13 标题称「**20 任务**」（实际 21）。
- `advice_record` 的裁决过程记录在 §11.1.1（E3，方案 (iv)），**但 §8 数据模型章节未增列** → 模型清单与实现不一致。

---

## §7 引用完整性：❌ 4 处断链

| # | 位置 | 引用 | 实况 |
| - | ---- | ---- | ---- |
| 1 | `docs/2026-09-16-competitive-learning-plan.md:4` | `docs/2026-09-15-lightfield-attio-rox-*.md` | ❌ **文件不存在**；实为 `2026-09-15-attio-lightfield-rox-three-way-comparison.md` |
| 2 | 同上 | `docs/2026-09-15-external-data-integration-*.md` | ❌ **文件不存在**；实为 `docs/2026-09-14-external-data-integration-design.md`（**日期为 09-14**） |
| 3 | 同文件第 3 行 | 自述为 `proactive-runtime-design.md`（**P8 待批准**）的来源附录 | ⚠️ 该文档**同日已废弃**；且「P8 待批准」与「最终设计已批准并实施至 S7」矛盾 |
| 4 | 最终设计 §1.3 第 5 行 | 「**§12 叙事**：只读观察期 → 回写 → 再谈接管」 | ❌ **§12 实为「铁律映射（统一）」**，全文无「叙事」章节 |

---

## §8 设计承诺 ↔ 实现落地：缺口清单

### 8.1 线A 补齐清单（§6.1，A-B1..A-B8）

| # | 补齐项 | 优先级 | 状态 | 证据 |
| - | ------ | ------ | ---- | ---- |
| A-B1 | `integration-providers` 描述符扩 `objects[]`/`token_mode`/`trust_level` | P0 | ❌ **未实现** | `tenantInstances.js` 对三者 **0 命中** |
| A-B2 | `credentialVault` 支持结构化凭据 | P0 | ❌ **未实现** | 对 `JSON.parse/appId/permanentCode` **0 命中** |
| A-B3 | `KIND_FACTORY` 加 `fxiaoke`/`neocrm` | P0 | ❌ **未实现** | 仍为 3 kind（generic-rest/mcp/cli）；fxiaoke 改置于 `src/sync/fxiaoke.js` |
| A-B4 | CAS 扩到字段值 | P0 | ✅ | `particleRepo.js` `casExpectField`（S4） |
| A-B5 | `connectorRouter` 增对象变化事件路由 | P1 | ❌ 未按设计落点 | `connectorRouter.js` 0 命中；能力由 `src/signal/followupRouter.js` 承接 |
| A-B6 | `integration-poll` 增对象增量拉取分支 | P1 | ❌ **未实现** | `timers.js:532` 的 `runOnce` 属 **researchScheduler**，非 sync engine |
| A-B7 | `monitorAccount` 复用为同步后重评 | P1 | 🟡 由 `followupEngine` 承接 | — |
| A-B8 | `updateParticle` 增 `patchMode:'deep'` | P2 | ❌ **未实现** | `particleRepo.js` 对 `patchMode` 0 命中 |

### 8.2 线A 新增清单（§7.1，A-N1..A-N6）

| # | 新增项 | 状态 | 备注 |
| - | ------ | ---- | ---- |
| A-N1 | 厂商无关同步内核 `src/sync/` | ✅ 模块齐（8 文件） | ⚠️ **命名漂移**：设计 `upsert.js`/`entityResolver.js` → 实现 `resolver.js` |
| A-N2 | `config_store['sync-mappings']` | ✅ `mapping.js` 读 | ⚠️ **未见播种 SQL**（仅代码引用） |
| A-N3 | `crm.external_ref` | ✅ `schema.sql:1083` | |
| A-N4 | 单向回写通道 `sync-writeback-fields` | 🟡 **已注册、无触发** | 见 §8.4 |
| A-N5 | `config_store['sync-trust']` | ✅ `trust.js` | |
| A-N6 | 同步可观测 `syncMetrics` | ✅ `src/monitor/syncMetrics.js` | |

### 8.3 线B 补齐清单（§6.2，B-B1..B-B9）

| # | 补齐项 | 状态 | 证据 |
| - | ------ | ---- | ---- |
| B-B1 | 挂载 `alertEndpoints` 8 端点 | ✅ | `routes.js:109/340` |
| B-B2 | 注册 `registerAlertHook` | ✅ | `alertSignalHook.js` + `server.js` |
| B-B3 | `alertStore` 由内存 Map 迁 DB | ⚠️ **偏离设计** | 设计「迁 DB + 建 `crm.alert` 表」；实现为**内存 Map + 写 `crm.signal`**，`crm.alert` **表未建**（0 命中）。功能等价且更简，但**属设计未同步的偏离** |
| B-B4 | 工作台第 7 视角（信号） | ✅ | |
| B-B5 | 首页信号卡 | ✅ | |
| B-B6 | 事件触发扩三源 | ✅ T12 | |
| B-B7 | 抽公共 mailer `src/mail/` | ❌ **未实现** | **`src/mail/` 目录不存在** |
| B-B8 | 复用 13 类规则 + `salesDailyScan` | ✅ | |
| B-B9 | 复用 `ready-queue-pump` 心跳 | ✅ | |

### 8.4 ⭐ 最严重：线A 全链路在 `src/` 下**无生产触发点**

| 检查项 | 结果 |
| ------ | ---- |
| `createSyncEngine` 的 `src/` 引用者 | **0**（仅 `scripts/smoke-sync-e2e.mjs`、`scripts/tmp-debug-engine.mjs`、`test/sync/*`） |
| `engine.runOnce` 的生产调用者 | **0**（`timers.js:532` 属 researchScheduler；`src/http/` 下 `runOnce`/`createSyncEngine` **0 命中**） |
| `sync-writeback-fields` Action 的生产调用者 | **0**（仅定义处 + 测试字符串） |
| `callWriteback` 的 `src/` 注入方 | **0**（仅 `engine.js` 内部形参与使用） |
| sync 相关 HTTP 端点 | **0**（`routes.js` 无 sync 路由；`/tenant-source-sync` 走 `discoveryOrchestrator`，非 sync engine） |
| `src/` 内唯一真实引用 | `src/signal/followupEngine.js:5` 引入 `createEntityResolver`（**仅 resolver，非 engine**） |
| S4 冒烟脚本实际覆盖面 | `smoke-writeback-e2e.mjs` 只测 **`updateParticle` 的 `casExpectField` 原语**，**未覆盖回写 Action，也未覆盖 engine 的 L3 分支** |

> **判定**：「S2 读入 / S4 回写已交付」在**代码模块层**成立，在**生产可触发层不成立**。
> 这与最终设计 §0.2 自己点名的病灶——「8 个查询端点**从未挂载**（文件头自述"等挂载方统一 add"——**挂载方一直没来**）」——**属同一缺陷家族**。
> **风险等级：高**。当前任何"已接入客户 CRM"或"能回写客户 CRM"的表述，都缺少生产可触发证据。

---

## §9 旁证文档的失效断言

`reports/nightly/20260915-audit.md`（09-15 夜批）含以下**当时正确、现已失效**的断言：

| 报告断言 | 现状 | 判定 |
| -------- | ---- | ---- |
| 最终设计「⛔ 未落地」；证据「`src/sync`/`src/signal`/`src/mail` 目录不存在；`db/schema.sql` 无 signal/standing_grant 表」 | `src/sync` **存在(8)**、`src/signal` **存在(11)**、`schema.sql:1028/1169` **有** signal/standing_grant | ❌ **失效**（false negative） |
| crm-coexistence「⛔ 未落地」；证据「`src/sync` 不存在，external_ref/sync_cursor 表未建」 | `src/sync` 存在；`external_ref:1083`、`sync_cursor:1106` 已建 | ❌ **失效** |
| 「`src/mail` 不存在」 | 仍不存在 | ✅ **仍为真**（对应 §8.3 B-B7 未交付） |

> 该报告是 **09-15 时点的正确记录**，但**未被 09-16 的 S1–S7 交付更新**。作为"反向假绿"（把已完成的报成未完成），会误导阅读者，建议加注或重生成。

---

## §10 建议处置（按代价从低到高）

| # | 动作 | 代价 | 收益 |
| - | ---- | ---- | ---- |
| 1 | 最终设计 §13 标题 `20 任务` → `21 任务`；§12 `6 张新表` → `7 张`（补 `advice_record`） | 2 行 | 消除内部矛盾 |
| 2 | 修 4 处断链（§7）：2 个文件名、1 处锚定改指最终设计、§1.3「§12 叙事」改指正确章节 | 4 行 | 可追溯性 |
| 3 | 废弃 banner 增补「**S 编号亦已重排**（S1–S7），不可用于定位」 | 4 文件各 1 行 | 消除 §5 错位风险 |
| 4 | 附录 B 补 4 条：3 条丢失修正 + 1 条范围反转（深度合并 → 红线） | ~6 行 | 修正记录完整 |
| 5 | 把 §4 丢失的 **5 项 Rox 借鉴项 + 2 条红线 + 叙事三句 + 1 条对外表述红线**迁移到有权威的去处（建议入 `competitive-learning-plan` 的观察清单，或最终设计新增附录） | 中 | 召回决策资产 | ✅ **2026-09-16 已执行** → 见 §10.2 |
| 6 | §8 DDL 三项回填（signal 5 列差异、standing_grant 2 列、advice_record 增列） | 中 | 模型与实现一致 |
| 7 | **为线A 补挂载点**（设计里已有：A-B6 `integration-poll` 增量分支 / A-B5 对象变化事件路由），或明确声明线A 为"模块就绪、待接入" | 高 | **消除最高风险** |
| 8 | 重生成或加注 09-15 夜批审计报告 | 低 | 消除失效红色断言 |

### 10.1 执行记录（2026-09-16，用户批准执行第 1–4 项）

| # | 状态 | 实际改动 |
| - | ---- | -------- |
| 1 | ✅ 已执行 | 最终设计 **8 处数字**：`:13` `T01–T20`→`T01–T21`；`:1039` §13 标题 `20 任务`→`21 任务`；`:259` / `:1024` / `:1027` / `:1030` / `:265` 各处 `6 张`→`7 张`；`:401` §8 标题加注「另有 1 张见 §11.1.1」；`:405` 新增合计 7 张的说明行；`:1614` 去掉失效的 `6 张` |
| 2 | ✅ 已执行（**并修复第 5 处**） | ① `competitive-learning-plan.md:3` 锚定改为最终设计（删「P8 待批准」）；② 同文件 `:4` 两个不存在文件名改为 `2026-09-15-attio-lightfield-rox-three-way-comparison.md` 与 **`2026-09-14-`**`external-data-integration-design.md`，并把 4 份废弃文档单列为「过程证据」；③ 最终设计 `:121` 「§12 叙事」→「§14.3 实施分段 + §15.1 #1」；④ **新发现第 5 处**：最终设计 `:13` 的「§11 的对照表」——§11 实为「授权面」，无任何对照表，已改指 §13 任务清单 |
| 3 | ✅ 已执行（**范围收窄为 3 文件**，原判 4 文件） | 证据：`S1–S5` 实际只出现在 **crm-coexistence（线A）** 与 **proactive-runtime（线B）** 两篇；`rox-benchmark` 的 `S1` 是**销售阶段分类**（`stageTaxonomy` S0/S0P/S1–S8），非交付分段；`three-way` 全文 **0 处 S 编号** → 前者各加「S 编号已重排 S1–S7、旧 S 不可用于定位」，`rox-benchmark` 改加**编号消歧**说明（销售阶段 vs 交付分段），`three-way` 无需加 |
| 4 | ✅ 已执行 | 附录 B **11 → 15 条**，补录 4 条：① Rox 冲突消解结论（旧 crm-coexistence 附录B 漏转录）② Agent Action 计价单元 P0 借鉴项**未承接**（追认为待重评估，非否决）③ Attio/Lightfield「取代」判定（依据已并入 §1.2）④ **交付范围反转**（旧 §7.2 深度合并/S5 → 终版 §15.2 #6 明确不做） |

**复核证据**：`grep` 残留旧数字/断链/错误交叉引用**均 0 命中**；`git status` 仅 5 个 `docs/*.md` 为 `M`。
**未闭合提醒**：`node scripts/validate-contract.mjs` 改动后仍 `valid:false` —— 报错项为**既有的 T21 契约缺口**（`review-gate.memory.read` 缺 `decision-retro`，`src/agent/agentSpec.js:53`），**与本次文档修正无关**，属代码侧一行修复，需另行批准后执行。

### 10.2 执行记录（2026-09-16，用户批准执行第 5 项：丢失资产回捞）

**迁移原则**：每项资产**只设一个权威归属**（单源），其余位置只放指针；全部标注「回捞」来历，避免被误读为新决策。

| 资产 | 原处（已废弃） | 新权威归属 | 落点 |
| ---- | ------------- | ---------- | ---- |
| 5 项 Rox 借鉴项（Agent Action 计价 / 「为什么」一键 / 反馈采集前端化 / Skills 受控自助 / 富化供应链） | `rox-benchmark` §5 | **`competitive-learning-plan` §6.1** | 带回 `file:line` 挂载点与边界，标注"未立项" |
| 2 条红线（warehouse-native 单租户 in-VPC 深部署 / Tether 类协议替代 MCP） | `rox-benchmark` §6 | **最终设计 §15.5（#10 / #11）** | §15 标题 `9 条`→`11 条`；**不重编号**（保住既有 §15.2 #6 等引用） |
| 叙事三句（对客户 / 行业评审 / 投资人） | `three-way` §9 | **最终设计附录 D.1** | 逐字保留 + 加**口径修正提醒**（见下） |
| 对外表述红线（数据不出租户边界） | `three-way` §8 #4 | **最终设计附录 D.2** | 与"取代叙事""AI 主动值守"合成禁用表述表 |
| 修正记录 4 条 | 两份废弃文档附录 B | 最终设计附录 B（第 12–15 行） | 见 §10.1 第 4 项 |

**回捞中新识别（超出原 §10 第 5 项范围，已一并登记并在报告中标注）**：

| 发现 | 处置 |
| ---- | ---- |
| `rox-benchmark` §7 两条可引用结论（"对手方证词"、"同一根轴两端"）在最终设计 **0 命中**（`金标准`/`同一根轴`/`放出流程`/`关进流程` 均 0） | 迁入**附录 D.3**（同属对外口径资产） |
| `rox-benchmark` **§8.5** 的另一份借鉴清单中 4 项未承接：校准预演安全阀（`预演` 0）、`fit_reason` 结构化（0）、拓客 Monitor/批量搜索（0）、给 M 补角色人变动信号（§1.3 #8 仅叙事） | 迁入**学习清单 §6.2**，标注"本次新识别" |
| **叙事第一句与已修正前提冲突**：原句"你不需要先有 CRM"，而同日已推翻为"客户**都有**自研或套装 CRM"（附录 B 第 1 行） | 附录 D.1 保留原句 + 加**使用前必读**（对外改讲"不需要搬迁"）——**不建议**改原句，改则失去逐字可追溯性 |
| `competitive-learning-plan` **§4** 状态列写 P0-A/P0-B"待用户批准"，与"已批准 + S1–S7 已交付"矛盾；**§5** 整表映射到已废弃文档的章节号 | §4 加**过期横幅**（指向 `proactive-s7-acceptance.md`）；§5 加**待重映射横幅**（保留 T/S 编号映射这一唯一仍有效部分）。**完整章节重映射未做**，属残留待办 |

**复核证据**：学习清单新增 §6 / §7 共两节；最终设计 §15.5 + 附录 D 已落；5 项借鉴项在最终设计仍**有意** 0 命中（设计只收"已决定要做"的内容），归属唯一指向学习清单 §6.1。

---

## §11 方法论备注（可复用）

本次比对采用的四条判据，均可复用：

1. **编号映射逐字核对**：不看"是否提到"，而比对**标题字符串是否逐字一致** → 才能发现"改名即断链"。
2. **丢失项双查**：一个结论若在**权威文档**与**新确认清单**中都零命中，才判定为"无家可归"；单处零命中可能只是换了说法。
3. **DDL 列清单三向比对**（废弃草案 / 最终设计 / 实际 schema）——比"表是否存在"更深一层。
4. **交付 ≠ 可触发**：模块存在、测试全绿、冒烟通过，都不等于**生产有触发点**。必须追问"谁在运行时调用它"（本次即由 `timers.js:532` 的 `runOnce` 属主误判引出，最终定位到线A 零接线）。

---

**— 报告结束 —**
