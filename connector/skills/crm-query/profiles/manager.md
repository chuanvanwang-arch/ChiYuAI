# crm-query · 经理视角（profiles/manager.md）

> 视角：经理（manager）用 crm-query 看「组合分布与汇总裁判」。

## 高频对话

- 「团队商机池分布？」→ 聚合：阶段分布/金额分布/赢率梯队
- 「本月回款预测？」→ 回款粒子聚合 + 逾期标记

## 查询边界

- 聚合类查询走读直连（默认放行）。
- 分布结果用业务语言（梯队/占比），不暴露原始 SQL/粒子 ID。