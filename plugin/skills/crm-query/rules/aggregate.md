# crm-query · 聚合规则（rules/aggregate.md）

## 聚合边界

- 组合分布（阶段/金额/赢率/回款逾期）→ 聚合查询走读直连。
- 返回 `{ type, items[], summary, ok }`；summary 为业务语言一句话（「12 条商机 3 条主攻 / 逾期回款 2 笔 80 万」）。

## 只读纪律

- 聚合不产生写：即使「预测回款」也仅读回款粒子 + 标记，不更新状态。
- 绝对禁删：无删除工具；「删商机/删客户」→ 拒绝，引导 crm-write（且写需两阶段+决策闸）。