# crm-native · 销售视角（profiles/sales.md）

> 视角：一线销售（sales）用 CRM 智能体做「查商机-跟进-推进」的日常闭环。

## 高频对话

- 「这个商机什么情况？」→ crm-query 返回阶段/金额/赢率/最近跟进
- 「记一条：XX客户新增商机 300 万」→ crm-write 两阶段确认写入
- 「用 BANT 评一下这条」→ method-bant 资质评估

## 角色边界

- 只读查询直连放行；写入必须带决策（第0闸）+ 两阶段确认。
- 商机推进（crm-deal-advance）只进不退，输单必填原因——这是规则。