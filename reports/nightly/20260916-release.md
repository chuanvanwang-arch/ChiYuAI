# CRM-ai-native 生产发布报告 · 2026-09-16 23:37（用户指令「请将最新版本上传到生产环境」）

## §0 结论先行

**发布成功（判定为「真发布」）**。生产由「落后本地 7 提交」对齐至发布源 `4aef591`；三容器 healthy、app 日志 0 ERROR、**HTTPS 未丢**、`.env` 完整。夜报 §6 遗留的三项实证缺口全部闭合。

| 项 | 结果 |
|---|---|
| 通道 | `release`（权威通道，非 hotfix） |
| 发布源 | 干净 worktree `.release-wt` @ HEAD `4aef591`（detached） |
| 自洽校验 | `verify-release-source.mjs` **EXIT=0**（启动链路自洽；11 处告警全在 `test/**`，既有技术债） |
| 命令 | `deploy-remote.py release --local-root .release-wt --password-file $TEMP\crm_ssh.pwd` |
| 退出码 / 耗时 | **0** / 29 秒（docker 分层缓存命中） |
| 新镜像 | `tencent-lighthouse-deploy-app` created **2026-09-16T15:36:57Z**（=23:36 CST） |

## §1 为何用「干净 worktree 快照」而非工作树发布

工作树为**脏**状态：46 `D`（专利/参赛目录重组持有）+ 6 `M` + 18 `??`。其中 `M src/agent/eventTrigger.js` 属并行会话在途改动（该文件 HEAD 版 `5c466b6` 已含三源矩阵，本地修改**未随本次发布**）。按 `crm-prod-release` SKILL 的通道判据，脏树整树打包会把 WIP 直接带上生产 → 故取 HEAD 快照发布。

发布前对快照的独立核对：`src/sync/exportGate.js`、`db/migration-signal-{config,owner-index}.sql`、`db/migration-sync-config.sql`、`src/signal/dispatcher.js`、`skills/` 全部 PRESENT；`db/migrate.js` 引用 32 个迁移、**缺失 0**；`pack-local.py:INCLUDE_DIRS` 含 `skills`。

## §2 缺口闭合验证（夜报 §6.2 三项实证缺失）

| 缺口（发布前实测） | 发布后实测 | 状态 |
|---|---|---|
| 无 `/app/src/sync/exportGate.js` | **PRESENT**（5012 B，镜像内 15:36） | ✅ |
| 无 `/app/db/migration-signal-config.sql` | **PRESENT** | ✅ |
| 无 `migration-signal-owner-index.sql` / `migration-sync-config.sql` | **PRESENT** | ✅ |
| `config_store` 无 `signal-delivery` / `signal-dispatch` | 键就位：`signal-delivery`（channels: inbox=on、im/email/webhook=off）、`signal-dispatch`（max_age_days=7）；另有 `sync-mappings` / `sync-trust` / `integration-providers` | ✅ |
| `exportGate` 代码引用 | 3 处 | ✅ |
| `skill_registry` | **24**（出厂声明 seed 16/16） | ✅ |
| 生产 `crm.signal_delivery` = 0 行 | 仍 **0**（`crm.signal` = 20，全 `open`） | 🟡 待观察 |

**关于 🟡**：本次已把该链路的**前置条件全部补齐**（出口泵源码 `exportGate.js`/`dispatcher.js` + `signal-delivery`/`signal-dispatch` 配置键 + 迁移）。投递行需由 `signal-dispatch` 定时器下一次 tick 生成，故不能在发布后即刻判绿——按「判据与链路不可同时静默」原则，此处以「前置闭合 + 投递待观」表述，不宣称已修复。

## §3 发布后点检

| 项 | 结果 |
|---|---|
| 三容器 | crm-app / crm-mcp / crm-pg **全 healthy**（CreatedAt 23:36:57，均已重建） |
| 表数量 | **73**（迁移全幂等，无变化） |
| 扩展 | `age 1.6.0` / `pgcrypto 1.3` / `pg_stat_statements 1.10` / `plpgsql`（注：`vector` 是 `crm` 域类型，不在 `pg_extension`） |
| app 启动日志 | `[error]`/`ERROR` 计数 **0**；`[AGE] decision network available=true`；`[skill-registry] seed inserted=0/16`、`memory applied=16/16` |
| `.env` 保留 | **16 键**；`PGPASSWORD=32` / `SMTP_PASS=30` / `CRM_LLM_SECRET=43`（与 09-11 基线一致 ⇒ `KEEP_RELATIVE_PATHS` 防复发生效） |
| HTTPS | **未丢**：`sites-available/crm` 含域名 `server_name` + `listen 443 ssl` + certbot 块；443/80 均有监听 |
| 证书 | `notAfter=Dec 5 00:06:09 2026 GMT`（有效） |
| 服务器内端点 | `oauth_meta=200`；`landing=200`；`Host: www.chiyuai.com` → **301**；**`tools/list` 无凭据 = 401 + `WWW-Authenticate: Bearer realm="crm-mcp", resource_metadata=…`** |
| 页面点检 | `/` 200、`/landing.html` 200、`/portal/my-todo.html` 200、`/signal-center.html` 200、`/agent-config.html` 200、`/system-overview/k` 302（登录跳转，预期） |
| 外网 | `http://81.70.184.198/landing.html` **200**、`/` **200**；`https://81.70.184.198/landing.html` **200**（须 `-k`：证书按域名签发，IP 直连会因校验失败显示 `000`）；`https://www.chiyuai.com/` **000**、`http://www.chiyuai.com/` **302** ⇒ **ICP 未备案的境内链路阻断，与本次发布无关** |

## §4 本次发现并已回写 SKILL 的三条判据更正

1. **「<60 秒 = 假发布」是错误判据**。docker 分层缓存命中时，**真实 release 也能 29 秒完成且表数不变**（迁移全幂等）。唯一充分判据 = 容器/镜像 `CreatedAt` 是否刷新 + 启动日志完整（本次两者均满足）。
2. **`/mcp` 鉴权探测体必须携带真实 `method`**。`-d '{}'` 无 `method` → `isAuthExempt()` 判定可豁免 → 落到 transport 层返回 **400**（不是 401），曾易被误读为「鉴权闸失效」；`initialize` 属**设计豁免**（200 正常，避免 CLI 登录入口死锁）。**闸有效性只用 `tools/list` 判定。**
3. **HTTPS 抹除已根治**。`nginx-crm.conf`（提交 `7cc511a`）随本次 release 上传后，`deploy.sh` 重写的配置自带域名/443/certbot 块 ⇒ 「每次 release 必修 HTTPS」降级为「30 秒判据 + 必要回退」。

## §5 遗留与待办

| # | 事项 | 状态 |
|---|---|---|
| 1 | `crm.signal_delivery` 仍 0 行 | 🟡 前置已闭合，待 `signal-dispatch` 定时器 tick 后复测 |
| 2 | 生产 `.env` 缺 `PGCRYPTO_SYM_KEY` | 既有缺口（非本次引入）：`credentialVault` fail-closed，S2 凭据保险库不可写 |
| 3 | 专利 / 参赛目录重组 + 并行会话 WIP（46 D + 6 M + 18 ??） | 未提交，待用户裁决（专利两树 35/36 字节级一致，属纯迁移） |
| 4 | GitHub 推送 | 未完成（沙箱出网阻断，本地领先 origin 109）→ `git push origin feat-multi-industry-meta-model` |
| 5 | 发布源快照 `.release-wt` | 建议用毕移除：`git worktree remove --force .release-wt` |

---

*生成：2026-09-16 23:45 · 自动化 ID `c49a0333-a9e1-4562-ac91-60b018003784` · 用户显式授权发布*
