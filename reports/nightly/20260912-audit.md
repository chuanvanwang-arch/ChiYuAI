# CRM-ai-native 夜间例行检查与发布审计报告（2026-09-12 22:00）

> 自动化 ID：c49a0333-a9e1-4562-ac91-60b018003784 · 8 项版
> 报告命名沿用 `<日期>-audit.md`（`reports/nightly/<日期>.md` 归属「夜批运行报告」自动化）

## 总览（结论先行）

| 项 | 结论 |
|---|---|
| ① 设计落地核对 | 本日（2026-09-12）`docs/` **无新增/修改设计文档**（0 份）→ 无落地核对任务；前序 09-11 线索公海池 T0–T7 已全落地 |
| ② 本地 GIT 提交 | 仅提交本报告（显式路径，禁 `add -A`/`rm`）；**24 个未提交删除（专利/IP 文档）持有不动**——IP 文档删除需用户确认 |
| ③ GitHub 推送 | 沙箱无 git 凭据 → **推送失败，待用户执行** `git push origin feat-multi-industry-meta-model` |
| ④ AI-* 10 SKILL | 10 基线完整、编号未变；本日无新通用方法论洞察 → **无需更新** |
| ⑤ 插件包 | `verify-plugin-zips.py` 两包全过；4 份 SKILL.md md5 一致 → **一致**；无重打包必要 |
| ⑥ 生产发布 | 生产只读 status 健康（3 容器 healthy / 61 表 / 16-16 skill）；**落后本地 9 commit**；自动发布未授权 → **不发布，待用户执行** |
| ⑦ KMD 探针 | 🟢6 🔴3(D1/D4/D6) 🟡3(D9/D10/D11) ⚪2；exit=1；D6 积压 70（最老 7.3 天）|
| ⑧ 系统概览采样 | `upserted 11 tenants`，exit=0，**成功** |

---

## ① 设计文档落地核对

按文件修改时间筛选 `docs/*.md`（2026-09-12 00:00 ~ 2026-09-13 00:00）：

```
find docs -name "*.md" -newermt "2026-09-12 00:00:00" ! -newermt "2026-09-13 00:00:00"
→ 命中 0 份
```

**结论**：本日无新增/修改设计文档，无需做落地对照。前序夜（2026-09-11 22:00）已用 Grep 锚点确认「线索公海池租户隔离 T0–T7」全落地无差距，本日无新设计承诺。

---

## ② 本地 GIT 提交

| 检查 | 结果 |
|---|---|
| `git log -1` HEAD | `f5ce73d`「docs: 夜间例行检查与发布审计报告（2026-09-11 22:00）」@ 2026-09-11 22:13 |
| 当前分支 | `feat-multi-industry-meta-model` |
| `git status --short` 计数 | 25 项 = **24 个未暂存删除** + 1 个未跟踪文件 |
| 分支状态 | 非 unborn；暂存规模正常（无数百文件异常）|

**持有未提交的改动（不自动提交）**：
- 24 个 ` D` = `docs/patents/2026-09-09/*` 与 `docs/2026-09-10-contest-submission-group-appeal-discovery.md`（专利/IP 文档删除）。按铁律与 09-11 夜批先例，**IP 文档删除需用户确认**，本自动化**不代为提交、不 `rm`**。
- 1 个 `??` = `reports/nightly/20260911.md`（「夜批运行报告」自动化产物，非本任务产物，不纳入）。

**本次提交**：仅本报告文件 `reports/nightly/20260912-audit.md`，显式路径 add、禁 `-A`、禁 `rm`。

```
git add reports/nightly/20260912-audit.md
git commit -m "docs: 夜间例行检查与发布审计报告（2026-09-12 22:00）"
```

| 分类 | 文件 | 说明 |
|---|---|---|
| 审计报告 | `reports/nightly/20260912-audit.md` | 本 8 项例行检查结构化报告 |

> commit hash 见末尾「执行元数据」。

---

## ③ GitHub 推送（origin = https://github.com/chuanvanwang-arch/ChiYuAI）

推送前 `git status` 确认：工作区含 24 个未提交删除（持有不动）。**说明**：`git push` 仅推送**已提交历史**（HEAD 及之前），未提交的 24 删除**不会进入远程**；但按夜批规则「工作区有未提交改动则先报告」，仅推送已提交部分。

**实测一次完整推送**（沙箱无凭据，预期失败）：

```
git push origin feat-multi-industry-meta-model
→ fatal: could not read Username for 'https://github.com': No such file or directory (PUSH_EXIT=128)
```

**结论**：推送未完成 / **需用户执行**。本地领先 origin 的提交（含本报告）在用户本地凭据环境推送：

```powershell
# 用户本地 PowerShell（沙箱无 git 凭据）
git push origin feat-multi-industry-meta-model
```

---

## ④ AI-* 10 个方法论 SKILL 更新

基线 10 个（编号固定，不可增删改）全部存在：

| # | SKILL | 存在 |
|---|---|---|
| 1 | ai-particle-system-design | ✅ |
| 2 | ai-ontology-vector-build | ✅ |
| 3 | ai-context-layering | ✅ |
| 4 | ai-memory-lifecycle | ✅ |
| 5 | ai-native-action-design | ✅ |
| 6 | ai-multi-agent-orchestration | ✅ |
| 7 | ai-event-driven-evolution | ✅ |
| 8 | ai-portal-page-generation | ✅ |
| 9 | ai-feedback-loop | ✅ |
| 10 | ai-capability-audit | ✅ |

**本日判定**：无新增设计文档、无新提交，当日工作**未产生新的通用方法论洞察**（法则/铁律/判据/模式）→ **无需更新**，未新增第 11 个、未含域名专有内容。

> 备注：`~/.workbuddy/skills/` 下另有 `ai-bypass-process-enhancement`、`ai-consultant-expert` 两个 `ai-*` 前缀技能，二者**均不属于 10 大能力基线**（前者为外部推理引擎旁路集成方法，后者用户明确「不在 10 大能力内，无关」），不影响基线编号完整性，本次未触碰。

---

## ⑤ 插件包一致性

`scripts/verify-plugin-zips.py` 校验结果（exit=0）：

| 插件包 | 状态 | 摘要 |
|---|---|---|
| `plugin/crm-native-plugin.zip`（09-11 22:01，162921 B） | ✅ 通过 | 133 文件；SKILL.md 20；含报价引擎/线索池/决策建议等路由断言全过 |
| `plugin-platform-admin.zip`（59157 B） | ✅ 通过 | 12 文件；v1.2.0；industry-onboarding 含 Step 4B/4.5/4C 与 discovery-rules 键断言全过 |
| 根目录 `crm-native-plugin.zip` / `crm-native-agent.zip`（08-31） | ⚠ 陈旧 | 仅提示，非主分发目录 |

**漂移交叉校验**：4 份 `skills/crm-native/SKILL.md` 副本（权威源 / `.workbuddy-plugin` / `connector` / `plugin`）md5 全部等于 `f4e9d2af31032d150de8024444601b69` → **内部零漂移**。

**结论**：插件包与当前 `src/`、skill 实现**一致**；本日 HEAD 未移动、无源码/SKILL 改动 → **无重打包必要**（不擅自执行打包）。

---

## ⑥ 生产版本对比与发布

### 生产只读 status（SSH 直连，未发布）

| 指标 | 生产（81.70.184.198）| 本地 HEAD（f5ce73d）|
|---|---|---|
| 容器健康 | crm-app healthy；`[skill-registry] seed inserted=16/16`、`memory applied=16/16` | — |
| HTTP | `127.0.0.1:3000`→200；`127.0.0.1:80`→200 | — |
| 表数量 | 61 | 61（schema.sql 同源）|
| DB 扩展 | age 1.6.0 / vector 0.8.6 / pgcrypto 1.3 | 同 |
| 落后本地 | **落后 9 个 commit**（自 09-11 22:00 起的 9 个功能线 commit 未发布，含线索公海池租户隔离 T0–T7 等）| HEAD=f5ce73d |

### 发布判定

- 自动发布**未授权**（历史多夜均「auto-release 未授权 → 未发布」）→ **本次不执行发布**。
- 工作树含 24 个未提交删除（专利 docs）。若直接 `release` 会按文件系统打包（`pack-local.py` 不读 git）→ 专利 docs 将从生产被移除；**须先由用户定夺删除**，或走干净 worktree 发布。

### 待用户执行的发布命令（PowerShell）

```powershell
$PY  = "C:\Users\wangchuan08\.workbuddy\binaries\python\envs\default\Scripts\python.exe"
$DIR = "D:\system\CRM-ai-native\scripts\tencent-lighthouse-deploy"
$PW  = "$env:TEMP\crm_ssh.pwd"

# 推荐：先隔离脏树再发布本机已提交历史（避免 24 删除被打包上生产）
git -C D:\system\CRM-ai-native worktree add --detach D:\system\crm-rel-tmp f5ce73d
& $PY "$DIR\deploy-remote.py" release --password-file $PW --local-root D:\system\crm-rel-tmp
& $PY "$DIR\deploy-remote.py" status --password-file $PW
git -C D:\system\CRM-ai-native worktree remove --force D:\system\crm-rel-tmp
```

> ⚠ 发布后必做：`deploy.sh` 会重写 nginx → 恢复 `server_name` 与 certbot HTTPS（见 `crm-prod-release` SKILL）；并核验 `.env` 键长（PGPASSWORD≈32 / SMTP_PASS≈30 / CRM_LLM_SECRET≈43）防假发布复发。禁在生产库执行 DELETE。

**结论**：发布未执行 / **需用户执行**。

---

## ⑦ KMD 闭环探针（只读巡检）

命令：`node scripts/kmd-closure-probe.mjs --json artifacts/kmd-probe-2026-09-12.json`（即 `npm run probe:kmd`）
JSON 产物：`artifacts/kmd-probe-2026-09-12.json` · exit=1（含 FAIL/ERROR）

| 探针 | 边 | 状态 | 关键指标 |
|---|---|---|---|
| D1 知识向量真伪 | K 构建 | 🔴 FAIL | 真模型向量占 **0%**（69/69 为 hash 伪向量）→ 语义检索≈随机 |
| D2 LK 知识层消费者 | ① K→D 供给 | 🟢 PASS | LK 4 处消费者（injector.js:45/50）|
| D3 L1 知识构成 | ① K→D 供给 | 🟢 PASS | 已注册知识 15.44%（69/447）|
| D4 outcome 真实性 | ⑤ 结果→D | 🔴 FAIL | 真自动回写 **0 条**（总计 4，种子 4）→ 业务结果从未回流 |
| D5 校准事件契约 | ⑥ 校准触发 | 🟢 PASS | 交集 1 项 outcome-set |
| D6 校准补丁积压 | ⑦ 参数生效 | 🔴 FAIL | **PENDING 70**，最老 **7.3 天** |
| D7 M→K 升格 | ⑧ M→K | 🟢 PASS | 1 条先例，1 由记忆升格 |
| D8 记忆可用性 | ② M→D 供给 | 🟢 PASS | 活跃可注入 145、锚定 72 |
| D9 噪声源排行(24h) | M 构建 | 🟡 WARN | 663 行，visit_shortfall 470（70.89%）|
| D10 D→K 回写 | ④ D→K | 🟡 WARN | 复盘产出知识 **0 条** |
| D11 知识投影契约 | K 构建 | 🟡 WARN | 仅 23.19% 能正确投影（16/69）|
| D12 结果自动回流 | ⑤ 结果→D | ⚪ SKIP | 需 `--e2e --db=test`（不写生产库）|
| D13 噪声闸门回归 | M 构建 | 🟢 PASS | 三关全过；近 60min 零 trace 噪声 |
| E2E 端到端哨兵 | ① K→D | ⚪ SKIP | 需 `--e2e --db=test` |

**汇总**：🟢 6 · 🔴 3 · 🟡 3 · ⚪ 2

### 需重点关注的 🔴/🟡

- **D1（🔴）**：真向量 0% 持续（与 09-11 同源）。verdict：「真模型向量占 0%（69/69 条为 hash 伪向量）→ 语义检索等于随机」。
- **D4（🔴）**：真自动回流恒 0（D4 双根因已于 09-11 08:1x 根治并 hotfix 上生产，但 `outcome_event_map` 仍需业务侧触发真实 outcome 才会计入；当前仍 0 条真自动）。
- **D6（🔴，积压治理）**：PENDING 70、最老 7.3 天（较 09-11 的 6.3 天继续恶化 → **校准在产出但无人消费**。

### D6 校准补丁分级待办（HITL 辅助，绝不自动应用）

命令：`node scripts/calibration-triage.mjs --json artifacts/calibration-triage-2026-09-12.json`（即 `npm run triage:calibration`）
JSON 产物：`artifacts/calibration-triage-2026-09-12.json` · exit=0

| 风险级 | 条数 | 样例 patch_id / knob / target |
|---|---|---|
| HIGH | 10 | `f45d379d`（source_refresh→realtime-pull, LOSS_REVIEW）、`32856bd8`（required_dims, QUOTE_PRICING）、`72576ee9`（edge_binding E1, LEAD_FOLLOW_UP）|
| MEDIUM | 32 | `bddee2ca`（source_refresh→real_time_streaming, OPP_QUALIFY）、`b2a2d547`（particle_attr_add, CLIENT_STRATEGY）|
| LOW | 28 | 其余 generated 补丁 |

> ⚠ **HITL 铁律**：校准补丁按铁律须经管理员审批流落地（`POST /api/calibration/patches/<patch_id>/approve` 或 `/api/my-todo/tune-approve`）。**本清单仅用于人工审阅消项，绝不自动应用**。

---

## ⑧ 系统概览采样（趋势累积）

命令：`node scripts/sample-system-overview.mjs`（默认连生产库 crm_native，localhost:5433，与 D7 探针同源）
默认输出：`[sample-overview] upserted 11 tenants` · exit=0

| 指标 | 结果 |
|---|---|
| 采样租户数 | 11（业务租户 + system 模板）|
| 三指标写入 | `k_method_skill`（方法类 SKILL 注册数）/ `m_precedent_edge`（先例边数）/ `d_l1_intercept`（L1 闸门拦截累计）各按租户 upsert |
| 幂等性 | 同 (tenant_id, sample_date, metric) 仅覆盖当日值，不新增历史行 |
| 退出码 | 0（成功）；`crm.system_overview_sample` 表已存在（09-11 收尾轮已建出）|
| 副作用 | 仅写采样表，零 DELETE、零业务表读写 |

**结论**：采样成功，为 `/system-overview/k`、`/m`、`/d` 三页 30 日趋势 SVG 累积真实时序数据。

---

## 阻塞与遗留

1. **GitHub 推送**：沙箱无凭据 → 待用户（命令见 ③）。
2. **生产发布**：自动发布未授权 + 工作树含 24 未提交删除 → 待用户（命令见 ⑥）。
3. **D6 校准积压恶化**（7.3 天）：须管理员走审批流消费，本自动化不自动应用。
4. **D1 / D4 红**：D1 真向量注入、D4 业务结果真实回流，均为持续性架构缺口，需专项治理（非夜批可消）。

---

## 执行元数据

- 日期：2026-09-12 22:00 (GMT+8)
- 分支：`feat-multi-industry-meta-model` · 报告基 HEAD：`f5ce73d`（本报告自身为该分支最新 commit，见 `git log -1`）
- 本报告 commit：本分支最新 commit（见 `git log -1`；基 HEAD 为 `f5ce73d`）
- 探针 JSON：`artifacts/kmd-probe-2026-09-12.json`
- 校准 triage JSON：`artifacts/calibration-triage-2026-09-12.json`
- 工作树遗留：24 个未提交删除（专利/IP，持有待用户确认）+ `reports/nightly/20260911.md`（夜批报告，非本任务）
