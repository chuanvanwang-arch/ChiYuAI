# 生产发布报告 · 2026-09-18 06:36–06:50（用户授权「提交并发布到生产系统」）

**结论先行**：**发布成功且已全项验证**。首次 release 因 **HEAD 半提交（`db/migrate.js` 引用未入库的迁移文件）** 致生产 `crm-app` 启动 ENOENT 崩溃（HTTP 000）；已定位、补提交、二次发布成功——三容器 healthy、镜像 `2026-09-17T22:42:53Z`、`.env` 16 键零回归、今晚 86 commit 全部上线并经特征探针实证。仍有 **4 个 commit 待推送 GitHub**（沙箱无凭据）。

---

## 一、提交（本地）

| # | hash | 内容 |
| ---- | ---- | ---- |
| 1 | `ef95d6f` | chore(materials): 素材归档重组——专利目录 `docs/patents` → `doc/patents`（36 文件**忠实迁移**：清单 36↔36 一致、md5 35/36 字节相同、1 个索引页有意更新）；参赛材料按赛事归类（10 旧件归档 + 33 新件，13MB，无单文件 >5MB） |
| 2 | `f261a66` | **fix(db): 补齐 `db/migrate.js` 已引用但未入库的迁移**（`migration-signal-contact-owner.sql`，2013B）——修半提交致生产启动 ENOENT 崩溃 |
| 3–4 | `d9a97ab` / `c71ca58` | 并行会话随后提交（探测拒因透传 + 设计文档收口），已含在本次发布源内 |

- 提交卫生：显式路径 add、禁 `-A`/`rm`；发布前工作树残留 10 M + 10 ?? 均为**并行会话在途**（`probes.js`/`verifyScope.js`/`channelRouter.js`/`test/setup/` 等，06:31–06:36 仍在写入），未代提交。

## 二、发布过程与事故处置

| 阶段 | 结果 |
| ---- | ---- |
| 发布源 | 干净 worktree `.release-wt` @ HEAD（`git worktree add --detach`，用毕 `remove --force`）；`verify-release-source.mjs` **EXIT=0**（启动链路自洽；5 处告警均为 `test/` 非启动链路） |
| 发布前基线 | 远程 `.env` 16 键 / PGPASSWORD 32·SMTP 30·LLM 43（长度含换行 33/31/44） |
| **第 1 次 release** | 🔴 `RELEASE_EXIT=1`，`deploy.sh` 在 `node db/migrate.js` 阶段 **exit=137（SIGKILL）**；生产 `crm-app` **Restarting 崩溃循环**，`http://127.0.0.1:3000` → **000** |
| 根因 | 容器日志：`ENOENT: /app/db/migration-signal-contact-owner.sql`（`db/migrate.js:57`）。即 **HEAD 半提交**——migrate.js 的引用已提交，`.sql` 本体仍是未跟踪文件 ⇒ 发布包内缺失 ⇒ 启动即崩。**`verify-release-source.mjs` 只校验 JS import，不校验 `db/migrate.js` 引用的 `.sql`**，故未拦下 |
| 修复 | `git add db/migration-signal-contact-owner.sql` → `f261a66`；复跑差集校验：**migrate.js 引用的全部 `.sql` 均在库（0 缺失）** |
| 第 2 次 release | ✅ `RELEASE_EXIT=0`，31 秒；migrate 全步通过、容器重建、本机与公网均 **200** |

## 三、发布后验证（全绿）

| 维度 | 结果 |
| ---- | ---- |
| 容器 | crm-app / crm-mcp / crm-pg **全 healthy** |
| 镜像 | `IMAGE_CREATED=2026-09-17T22:42:53Z`（刷新 = 真发布，非假发布） |
| `.env` | **16 键不变**，PG 33 / SMTP 31 / LLM 44（与发布前完全一致，**防复发验证通过**） |
| 数据库 | 73 表；扩展 age 1.6.0 + pgcrypto 1.3 + pg_stat_statements 1.10 |
| 迁移数据面 | `closed_reason='superseded-by-personal-isolation'` **= 201 行**（个人隔离迁移生效，**零 DELETE**）；`migration-signal-personal-scope.sql` 与 `migration-signal-contact-owner.sql` 均在容器内 |
| 特征探针 | `closeStaleAggregates` 3 文件 / `channelProvider` 9 文件 / `ensureTenantScenarioSafely` 2 文件；`channel-adapters.html`、`crm-sync-console.html`、`probes.js`、`ics.js`、`exportGate.js`（实为 `src/sync/`）全部就位；容器 `skills/` 20 目录、`skill_registry` 16/16 |
| 页面点检 | `/`、`/landing.html`、`/portal/my-todo.html`、`/signal-center.html`、`/channel-adapters.html`、`/crm-sync-console.html` **全 200** |
| 通道闸 | `/mcp` + `Accept: application/json, text/event-stream` + `tools/list` → **401 + `WWW-Authenticate: Bearer realm="crm-mcp"`** ✅（注：不带头会返 **400**，非闸门失效） |
| 出口泵 | `crm.signal_delivery` **202 行全部 `sent`**（无 undelivered）；`crm.signal` 216（open 15） |
| 日志 | 近 8 分钟 `error|failed` 计数 **0** |
| 回滚需求 | 无 |

## 四、遗留与待办

| 事项 | 状态 |
| ---- | ---- |
| GitHub push（4 commit：`ef95d6f`/`f261a66`/`d9a97ab`/`c71ca58`） | 🔴 **需用户执行**（沙箱 `could not read Username`，PUSH_EXIT=124）：`git push origin feat-multi-industry-meta-model` |
| 并行会话在途（10 M + 10 ??） | ⚪ 未纳入本次发布；其后续提交需再发一次 release 才能上生产 |
| `verify-release-source.mjs` 缺口 | 🟡 建议补一条「`db/migrate.js` 引用的 `.sql` 是否全部入库」断言（当前须手动 `comm` 差集），已记入 `crm-prod-release` SKILL |
| 遗留告警 | `PGCRYPTO_SYM_KEY` 未在 compose env 展开（库内密文可解，属既有形式告警）；compose `version` 属性过时（提示级） |
