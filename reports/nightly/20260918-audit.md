# 2026-09-18 夜间例行检查与发布报告（8 项）

> 执行时间：2026-09-18 23:02–23:20（GMT+8）｜分支 `feat-multi-industry-meta-model`｜起始 HEAD `e16a9d1` → 结束 HEAD `823f020`

## §0 结论先行

| # | 项 | 结论 | 关键数字 |
|---|---|---|---|
| ① | 设计文档落地 | 🟢 实质差距 1 条 | 当日 6 份文档；4 份「待批准/研究类」按 HARD-GATE 正确未实施，**不计缺陷** |
| ② | 本地 GIT 提交 | 🟢 **11 commit** | 9 功能线 + 文档 + 报告；显式路径 add，禁 `-A`/`rm`；目标测试 **126/126 绿** |
| ③ | GitHub 推送 | 🔴 **失败（凭据层）** | `could not read Username`（EXIT=128）；github:443 本次**可达(200)**；**领先 origin 53 提交，需用户执行** |
| ④ | AI-* SKILL | 🟢 10 基线完整，增量补强 3 | ontology-vector / particle-system / native-action；禁词扫描零污染 |
| ⑤ | 插件包 | 🟡 `verify` EXIT=0，但**源已前进** | 包为 09-17 21:15；**缺 09-18 新增 `dealer-sales-partner` 专家与 `channel-agent` 智能体** |
| ⑥ | 生产发布 | 🟡 **落后 26 提交，未发布** | 镜像 `2026-09-18 17:06:56` ≈ `8535f13`；无持续授权 → 命令已给 |
| ⑦ | KMD 探针 | 🟢9 🔴2 🟡4 ⚪2（exit=1） | 🔴 D6 积压 71/13.3 天、**D14 embedding 402 复发**；🟡 D15 L1 查询侧维度不匹配（**本次已随 ② 修复并提交**） |
| ⑧ | 概览采样 | 🟢 exit 0 | `upserted 13 tenants`，幂等成功 |

**两处需用户立即处置**：① SiliconFlow 余额再次不足（D14 🔴，与 09-18 上午同源故障复发）；② D6 校准补丁 71 条 HITL 审阅（最老 13.3 天，连续第七夜恶化）。

---

## §1 设计文档落地对照

| 文档 | 性质 | 承诺改动 | 落地状态 | 证据（file:line / commit） |
|---|---|---|---|---|
| `docs/2026-09-18-dealer-portal-design.md`（v2 已批准） | 设计 | 1:N 经销商租户联邦、跨租户只读授权、撞单检测/仲裁、平台总闸、渠道管理页、Buddy 模式 | 🟢 **已落地** | `src/federation/scope.js:9` `federationReadScope`；`src/federation/config.js:13` 总闸；`src/http/dealerRoutes.js:3`（170 行，6 端点）；`src/web/channel-admin.html`（16.5KB）；`src/portal/layoutMenu.js:51` 角色限定；commits `cc3a67c`/`5626277`/`57dc8fe`/`f221cb2`/`4409a7f`/`2c68799` |
| `docs/2026-09-18-unified-integration-design-v2.md`（待批准） | 设计 | 接入三形态 + 回写对账；四类闸门真接线 | 🟡 **部分落地** | 已落地：P0-1 `21e28de`、P0-2 `843db76`/`8535f13`、P0-3 `44380d2`、P0-4 `70e3bdf`、P2 三形态 `7d2fd88`、P3 四类动作 `src/channels/reviewGate.js:17-19`（本次 `f824123`）；**未落地：P4/P7** |
| `docs/2026-09-18-integration-unified-merged.md` | 合并版研究 | 竞品研究 + 统一设计 + P0 借鉴合并 | 🟢 已落地（文档类） | 本次提交 `4a34e70` |
| `docs/2026-09-18-channel-onboarding-connector-first-design.md`（**待批准**） | 设计 | 连接器优先 + 本机桥接，凭据不进平台 vault | 🟢 按 HARD-GATE **正确未实施** | 对应已实现子集：`src/channels/sourceKinds.js`、`src/web/onboarding-guide.html`、`src/connectors/discovery/credentialVault.js` |
| `docs/2026-09-18-competitor-crm-integration-study.md` | 竞品研究 | 无代码承诺 | ⚪ 不适用 | — |
| `docs/2026-09-18-hardware-sales-pain-points-mapping.md`（v3 定稿） | 痛点映射研究 | 无代码承诺 | ⚪ 不适用 | — |

### 差距清单（1 条实质）
1. **P4/P7 同步闸门生产调用点未接**（`unified-integration-design-v2` §0 自评 🟡）：`gate.js` 路由与 `reviewGate` 四类动作映射已真（`src/sync/gate.js:5`、`src/channels/reviewGate.js:17-19`），但 `mapping-change` / `trust-elevate` 在 `src/` 内**除定义方外零调用点**（grep 实证）。风险：风险动作仍可能绕过闸门——**在放开客户侧回写前必须先接通**（设计内定序 P3→P6）。

---

## §2 本地 GIT 提交（11 commit）

提交前跑目标测试 **10 文件 126/126 全绿**（含 L1 查询维度回归锁、闸门接线守卫、复盘升格、四个页面契约）。

| 序 | hash | 分类 | 内容 |
|---|---|---|---|
| A | `9af4d12` | L1 消费面契约 | 查询侧禁用 `hashVector`（384≠存储 1024）；名称归位走精确匹配；embedding 启动引导 |
| B | `f824123` | 同步闸门 | `reviewGate` 覆盖 mapping-change/trust-elevate/enable-writeback（原只认 first-connect）；`connectorRouter:79`/`timers:648` 注入 `createSyncGate` + 接线守卫测试 |
| C | `3fe5bd0` | 决策复盘 | `retroPromote.js` 复盘结论升格为知识（D→K 第四条边）+ 真实环境回归 |
| D | `496e8b8` | 探针/基建 | KMD 探针新增 L1 消费面与维度判据；vitest 环境隔离 |
| E | `0ed9605` | 前端呈现层 | 拓客/发现/规则/同步控制台/通道页/首页修复 + 渲染产物契约测试（ui-lint 零违规） |
| F | `400bd64` | 配置化 | 拓客与发现规则阈值 100% 配置化（禁硬编码）+ 活动派生 |
| G | `126cc0d` | 校验脚本 | 4 个渲染产物校验脚本（运行期渲染态为判据，静态 grep 不构成证据） |
| H | `4a34e70` | 设计文档 | 当日 5 份设计/研究文档入库 |
| I | `679c6e8` | 报告 | 通道真实凭据验证、门户提交清单、发现页清理 + 0917/0918 夜批报告 |
| J | `823f020` | 对外素材 | 销售平台介绍材料与印刷版同步（含 `doc/event` 已跟踪件） |

**持有未提交（3 项临时件 + 111 项参赛重组）**：
- `.tmp-chk0.mjs`、`.tmp-results/`、`hd.txt` —— 临时产物，不动（禁 DELETE）。
- `doc/参赛2/**` **81 D + 30 ??** —— 参赛材料目录重组含**批量删除**，按铁律「禁 DELETE + 需用户确认」**持有**，未提交。请用户确认后我再按功能线提交。

**异常检查**：HEAD 存在（非 unborn），本次暂存规模 10–12 文件/批，无巨型根 commit 风险。

---

## §3 GitHub 推送

- 结果：🔴 **失败**。`GIT_TERMINAL_PROMPT=0 timeout 90 git push origin feat-multi-industry-meta-model` → `fatal: could not read Username for 'https://github.com'`，**EXIT=128**。
- 失败模式：**凭据层**（与 09-16/09-17 的「出网静默阻断 124」不同）。本次 `curl https://github.com` 返回 **200**，网络可达，纯缺凭证。
- 推送前 `git status` 已确认：除上述持有项外无未提交改动。
- **领先 origin 53 提交。需用户本地执行（PowerShell）：**

```powershell
cd D:\system\CRM-ai-native
git push origin feat-multi-industry-meta-model
```

---

## §4 AI-* 10 大能力 SKILL

基线校验：10 个 SKILL 全部在位（particle-system-design / ontology-vector-build / context-layering / memory-lifecycle / native-action-design / multi-agent-orchestration / event-driven-evolution / portal-page-generation / feedback-loop / capability-audit），**未新增第 11 个、未改编号**。

增量补强 3 处（均为中性通用表述，禁词扫描零命中）：

| SKILL | 新增 | 要点 |
|---|---|---|
| `ai-ontology-vector-build` | 金律 22 | **存储面绿 ≠ 消费面通**：写入侧换真模型而查询侧仍用另一套构造 ⇒ 维度/空间不同 ⇒ 「存了永远查不到」，而覆盖率/维度判据全绿。判据＝跑一次真实召回；维度常量单一事实源；精确归位不依赖向量；回填脚本自身也会假绿（非空伪向量致 `candidate=0`） |
| `ai-particle-system-design` | 金律（跨主体只读授权） | 1:N 共享需求**不新增主体类型**：① 共享关系存配置 ② 作用域解析为唯一内核点 ③ 写入侧物理拦截越界。**惰性退化**（无配置 → 仅自身主体）＝零侵入闸门；总闸平台级默认关；只读可共享、写必须留在本主体；作用域返回形状须测试锁死 |
| `ai-native-action-design` | 反模式 8 | **闸门只认一个动作 = 枚举覆盖不全 = 静默放行**；闸门动作枚举须与风险动作集合一一对应且各有负向用例；**闸门路由存在 ≠ 闸门被调用**（判据＝生产调用点）；拒绝原因必须透传到用户可见面，否则把可定位失败变成持续人工消耗 |

---

## §5 插件包

| 包 | 时间戳 | 状态 | 差异摘要 |
|---|---|---|---|
| `plugin/crm-native-plugin.zip` | 09-17 21:15（168786 B，133 项） | 🟡 **落后（内容面）** | 结构校验 EXIT=0（name/version=1.12.0/20 个 SKILL.md/源↔包一致）；但**不含 09-18 新增的 `dealer-sales-partner` 专家与 `channel-agent` 智能体**（包内 grep 零命中） |
| `plugin-platform-admin.zip` | 09-17 21:15 | 🟡 同上 | 同上；`plugin-platform-admin/` 源目录亦为 09-17 |
| `dealer-sales-partner.zip` | 09-18 13:24 | 🟢 新增 | 当日新专家包，无对照基线 |
| `crm-native-agent.zip` | — | ⚪ 不存在 | 已更名/并入 `crm-native-plugin.zip` |

**建议（未执行）**：新增经销商能力后应重打两个插件包，并补一条守卫——**包内智能体/SKILL 集合须等于源目录集合**（当前 `verify-plugin-zips.py` 只校验已存在项的一致性，**缺项不构成失败**）。

---

## §6 生产版本对比与发布

| 项 | 值 |
|---|---|
| 三容器 | `crm-app`/`crm-mcp`/`crm-pg` 全部 **Up 6 hours (healthy)**，CreatedAt `2026-09-18 17:06:56` |
| 镜像对应提交 | ≈ `8535f13`（16:43，17:06 前最后一个提交） |
| 本机 HEAD | `823f020` |
| **落后** | **26 提交** |
| 健康面 | 表 **74**（昨夜记录 73，+1）；扩展含 `age 1.6.0`/`pgcrypto 1.3`/`vector`；`/` 与 `:80` 均 **200**；`[AGE] available=true`；`skill-registry 16/16` |
| 特征签名 | 容器内**无** `/app/src/federation/`、`dealerRoutes.js`、`channel-admin.html`、`embeddingBootstrap.js`；`assembler.js` 仍含 `hashVector`×2 ⇒ **经销商联邦、L1 消费面修复、复盘升格均未上线** |
| ⚠ 已知 | `PGCRYPTO_SYM_KEY` 未设置（vault fail-closed，沿用既有红线） |

**发布判定**：改动含 `src/` 源码 + 前端 + 测试 → 按 SKILL 走 **release** 主通道。**无持续自动发布授权 ⇒ 未执行**。请用户确认后本地执行（PowerShell）：

```powershell
cd D:\system\CRM-ai-native
git worktree add --detach .release-wt 823f020
node scripts/verify-release-source.mjs .release-wt        # 期望 EXIT=0
$PY = "C:\Users\wangchuan08\.workbuddy\binaries\python\versions\3.13.12\python.exe"
& $PY scripts\tencent-lighthouse-deploy\deploy-remote.py release --password-file "$env:TEMP\crm_ssh.pwd" --local-root .release-wt
& $PY scripts\tencent-lighthouse-deploy\deploy-remote.py status  --password-file "$env:TEMP\crm_ssh.pwd"
git worktree remove --force .release-wt
```

> 注：工作树仍脏（111 项参赛重组 + 3 临时件）→ **不可直接按目录打包**，必须走 `--local-root` 干净 worktree。另：hermes venv 缺 `paramiko`，须用上面的托管 python 路径。

---

## §7 KMD 闭环探针（只读巡检 17 条，exit=1）

产物：`artifacts/kmd-probe-2026-09-18.json`
**数据源口径更正（沿用 09-16）**：探针 `kmd-closure-probe.mjs` 连 `localhost:5433/crm_native`——实为**本机开发库**（WSL 转发），非生产 VM `crm-pg`。下表指标为本机口径。

| 探针 | 边 | 状态 | 关键指标 |
|---|---|---|---|
| D0 环境基线漂移 | — | 🟢 | `vector(1024)`、`drift_items=0` |
| D1 知识向量真伪 | K 构建 | 🟢 | 真向量 **100%**（72/72，`max_nonzero=1024`，`has_negative=72`） |
| D2 LK 知识层消费者 | ① K→D | 🟢 | 4 处消费者（`injector.js:45,50`） |
| D3 L1 知识构成 | ① K→D | 🟢 | 已注册知识 6.75%（72/1067） |
| D4 outcome 真实性 | ⑤ 结果→D | 🟡 | 链路就绪（规则 4/4 enabled）但真回流 0，`event_emission_ok=false` |
| D5 校准事件契约 | ⑥ 校准触发 | 🟢 | 交集 1 项 `outcome-set` |
| **D6 校准补丁积压** | ⑦ 参数生效 | 🔴 | **PENDING 71，最老 13.3 天** |
| D7 M→K 升格 | ⑧ M→K | 🟢 | 1 条先例，1 条由记忆升格 |
| D8 记忆可用性 | ② M→D | 🟢 | 可注入 191 / 锚定 142 |
| D9 噪声源排行(24h) | M 构建 | 🟡 | 2312 行（量门 5000），`visit_shortfall` 1510 行 65.31% |
| D10 D→K 回写 | ④ D→K | 🟡 | 复盘产出知识 **0** 条（本次已提交 `retroPromote`，待下一夜观测） |
| D11 知识投影契约 | K 构建 | 🟢 | 61.54%（16/26） |
| D12 结果自动回流 | ⑤ 结果→D | ⚪ SKIP | 需 `--e2e --db=test` |
| D13 噪声闸门回归 | M 构建 | 🟢 | 三关全过，近 60 分钟零 trace 噪声 |
| **D14 外部依赖活性** | K 构建 | 🔴 | **embedding HTTP 402 余额不足**（复发） |
| **D15 L1 消费面可达** | ④ 上下文注入 | 🟡 | 名称归位可用，**语义召回不可用**（查询侧 384 ≠ 存储 1024） |
| E2E 端到端哨兵 | ① K→D | ⚪ SKIP | 需 `--e2e --db=test` |

**汇总：🟢9 🔴2 🟡4 ⚪2，真实退出码 = 1。**

### 需高亮项（verdict 原文）
- **D14（🔴，P0）**：`embedding=http-402  embedding_detail=Embedding 失败: Embedding HTTP 402：Sorry, your account balance is insufficient`。**与 09-18 上午同源故障复发**——余额恢复后仅一天即再次耗尽。影响：新知识粒子向量将全部静默降级。**需用户充值**。
- **D6（🔴）**：`PENDING=71  REJECTED=1  ROLLED_BACK=1  APPLIED=1  pending_age_days=13.3`。积压龄连续第七夜恶化（6.3→7.3→8.3→10.3→11.3→12.3→**13.3**）。
- **D15（🟡，本夜已修复）**：`provider=model  query_side_dim=384  expect_dim=1024`。根因修复已在本次 commit `9af4d12`（`assembler.js` 不再以 `hashVector` 构造查询向量）；探针在提交前运行，故仍报 🟡。**下一夜应转绿，作为修复有效性判据。**

### D6 校准分诊（`artifacts/calibration-triage-2026-09-18.json`）
**LOW 29 · MEDIUM 32 · HIGH 10 · 总 71**。
HIGH 风险集中在三类：`required_dims`（维度不全，6 条，涉及 CLIENT_STRATEGY/QUOTE_PRICING/SOLUTION_VALUE/OPP_QUALIFY）、`source_refresh`（刷新策略，2 条）、`edge_binding`（先例边绑定，2 条）。最老 HIGH 创建于 09-05。

> ⚠ **HITL 铁律**：校准补丁**必须经管理员审批流落地**（`POST /api/calibration/patches/<patch_id>/approve` 或 `POST /api/my-todo/tune-approve`）。本清单**仅用于人工审阅消项，绝不自动应用**。

---

## §8 系统概览采样

- 命令：`node scripts/sample-system-overview.mjs` → **`[sample-overview] upserted 13 tenants`**，**exit 0**。
- 表 `crm.system_overview_sample` 已存在，复合 PK `(tenant_id, sample_date, metric)` 幂等覆盖，未新增历史行；仅写采样表，零业务表触碰、零 DELETE。
- 三指标 `k_method_skill` / `m_precedent_edge` / `d_l1_intercept` 各 13 行（13 租户 × 3）。租户数 13 与昨夜持平。
- 产物随本报告引用：`artifacts/kmd-probe-2026-09-18.json`、`artifacts/calibration-triage-2026-09-18.json`。

---

## §9 待用户处置清单（按优先级）

| P | 事项 | 动作 |
|---|---|---|
| P0 | SiliconFlow 余额再次不足（D14 🔴） | 充值；充值后本机重嵌 + 复核 D14 转绿 |
| P0 | 校准补丁 71 条 HITL 审阅（最老 13.3 天） | 按 triage 分级消项，走审批流，不得自动应用 |
| P1 | GitHub 推送（领先 53） | 本地执行 §3 PowerShell 命令 |
| P1 | 生产发布（落后 26） | 确认发布清单后执行 §6 命令 |
| P1 | `doc/参赛2` 目录重组（81 D + 30 ??） | 确认删除范围后我再分组提交 |
| P2 | P4/P7 同步闸门生产调用点未接 | 放开客户侧回写前必须先接通 |
| P2 | 插件包重打（缺 09-18 新增专家/智能体） | 重打两包 + 补「集合相等」守卫 |
| P3 | 探针连接目标硬编码 `localhost:5433` | 改为可配置，消除「连生产库」口径误判 |
