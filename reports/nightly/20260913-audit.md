# 夜间例行检查与发布审计报告（2026-09-13 22:00）

> 自动化 ID：c49a0333-a9e1-4562-ac91-60b018003784 · 工作目录 D:\system\CRM-ai-native
> 分支 `feat-multi-industry-meta-model` · HEAD `0767022` · 报告沿用 `-audit` 后缀（避让「夜批运行报告」自动化占用的 `20260913.md`）

## 摘要（结论先行）

| # | 项 | 结论 | 关键指标 |
|---|---|---|---|
| ① | 设计文档落地 | 本日 docs/ **0 份**新增/修改设计文档 → 无落地核对 | 前序线索公海池 T0–T7 已全落地（09-11 确认） |
| ② | 本地 GIT | HEAD `0767022`；工作区 26 项（24 `D` 专利/IP 持有 + 2 `??` 历史 nightly 报告）；**本次仅提交本报告 1 commit** | commit `见文末` |
| ③ | GitHub 推送 | 沙箱无凭据 → **失败，待用户** | PUSH_EXIT=128 |
| ④ | AI-* 10 SKILL | 10 基线完整、编号未变；本日无新通用方法论洞察 → **无需更新** | — |
| ⑤ | 插件包 | verify 两包全过；4 份 crm-native SKILL.md md5 一致（`f4e9d2af`）；根目录 2 旧包（08-31）仅提示 | 一致 |
| ⑥ | 生产版本 | SSH status 健康（3 容器 / 16/16 skill / 61 表 / age 1.6.0+vector 0.8.6 / HTTP 200）；**缺 lead-pool-config → 落后本地 9 commit**；auto-release 未授权 → **未发布，已给命令** | 待用户 |
| ⑦ | KMD 探针 | 🟢6 🔴3(D1/D4/D6) 🟡3(D9/D10/D11) ⚪2；exit **1**；D6 积压 70 最老 **8.3 天**（较 09-12 的 7.3 天恶化） | JSON 见 artifacts |
| ⑧ | 系统概览采样 | `upserted 11 tenants`，exit 0，**成功** | 表已存在 |

---

## ① 设计文档落地排查

**本日（2026-09-13）`docs/` 无新增/修改设计文档**（find mtime 0 命中；`git log --since/until 09-13` 0 提交）。无"设计/方案/架构/brainstorming/writing-plans"性质文档需核对落地。

前序待办（非本日新增，仅复核）：线索公海池 S0/S0P 租户隔离 T0–T7 已于 09-11 22:00 夜批经 Grep 锚点确认全落地，本日无回退。

→ **差距清单：无。**

---

## ② 本地 GIT 提交

- `git log -1` → `0767022 docs: 夜间例行检查与发布审计报告（2026-09-12 22:00）`（HEAD 存在，非 unborn）。
- `git status --short` → **26 项**：24 个 `D`（docs/patents/2026-09-09/*、docs/2026-09-10-contest-submission-group-appeal-discovery.md，IP/竞赛文档**删除**）；2 个 `??`（reports/nightly/20260911.md、reports/nightly/20260912.md，归属「夜批运行报告」自动化，非本任务产物）。
- **提交策略（遵循铁律）**：
  - 24 个 `D`（专利/IP 删除）**持有不动**——IP 文档删除需用户显式确认，连续 3 夜一致处理。
  - 2 个 `??` 历史 nightly 报告**不纳入**（属另一自动化管理）。
  - 本自动化唯一可提交交付物 = 本报告 → **显式路径 add，单 commit**。

**本次提交**：
- `git add reports/nightly/20260913-audit.md artifacts/kmd-probe-2026-09-13.json artifacts/calibration-triage-2026-09-13.json`
- commit message：`docs: 夜间例行检查与发布审计报告（2026-09-13 22:00）`（含 KMD 探针与校准 triage JSON 证据）
- **新 HEAD**：`见文末执行记录`（提交后补全）

> 提交后工作区仍余 24 `D`（持有）+ 2 `??`（夜批报告），均为有意不提交项。

---

## ③ GitHub 推送

- 前提：第 ② 步本地提交已完成（仅本报告 commit）。
- 执行 `git push origin feat-multi-industry-meta-model` → **沙箱无 git 凭据，失败**（proxy/github 不可达，PUSH_EXIT=128）。
- **待用户本地执行**（PowerShell，仓库根目录）：
```powershell
git push origin feat-multi-industry-meta-model
```
> 标注：**推送未完成 / 需用户执行**。

---

## ④ AI-* 10 SKILL 复核

- 10 个基线 SKILL 全部存在、编号未变：
  `ai-particle-system-design` / `ai-ontology-vector-build` / `ai-context-layering` / `ai-memory-lifecycle` / `ai-native-action-design` / `ai-multi-agent-orchestration` / `ai-event-driven-evolution` / `ai-portal-page-generation` / `ai-feedback-loop` / `ai-capability-audit`。
- 本日 0 提交、0 新设计文档，无新的通用可复用方法论洞察（法则/铁律/判据/模式）产生。
- **结论：无需更新**（备注 `ai-bypass-process-enhancement` / `ai-consultant-expert` 属基线外，未触碰）。

---

## ⑤ 插件包一致性检查

| 插件包 | 状态 | 差异摘要 |
|---|---|---|
| `plugin/crm-native-plugin.zip` | 一致 | `verify-plugin-zips.py` 全过；4 份 crm-native SKILL.md（权威源/.workbuddy-plugin/connector/plugin）md5 均 `f4e9d2af`，byte-equal |
| `plugin-platform-admin.zip` | 一致 | `verify-plugin-zips.py` 全过（version 1.2.0，12 文件，industry-onboarding 各 Step 在源）；与 `plugin-platform-admin/` 同源 |
| 根目录 `crm-native-plugin.zip` / `crm-native-agent.zip` | 陈旧（仅提示） | 08-31 打包，落后于 `plugin/` 主分发目录；与 09-11/09-12 一致，非回归 |

- 关键提醒（沿用 09-11 经验）：`verify-plugin-zips.py` 仅校验结构/内容断言，**不校验包 vs 源鲜度**；本次已另行做 4 副本 md5 比对消除误判。
- HEAD 自 09-12 起未移动 → 无需重打包。
- **结论：两主分发包一致；不擅自打包（用户未明确要求）。**

---

## ⑥ 生产版本对比与发布

**环境**：SSH `ubuntu@81.70.184.198`，密码文件 `%TEMP%\crm_ssh.pwd`（存在）；远程根 `/opt/crm-ai-native`；容器 `crm-app`(3000)/`crm-mcp`(3001 回环)/`crm-pg`(AGE)。

**生产只读 status（本次实测，SSH 可达）**：
- 容器：`crm-pg` healthy（Up 2 days）；`crm-app` / `crm-mcp` 启动日志正常，`[skill-registry] seed inserted=16/16`、`memory applied=16/16`、`[AGE] decision network available=true`。
- 自测：`http://127.0.0.1:3000/ → 200`、`http://127.0.0.1:80/ → 200`。
- 扩展：`age 1.6.0` / `pgcrypto 1.3` / `plpgsql 1.0` / `vector 0.8.6`。
- 表数量：**61**。

**版本差异表**：

| 维度 | 本地 (HEAD `0767022`) | 生产 (81.70.184.198) | 差异 |
|---|---|---|---|
| commit | `0767022`（含 09-11 22:00 起 **9 个未发布** lead-pool commit） | 落后本地 9 commit | ❌ 线索公海池未发布 |
| lead-pool 代码 | `/app/src`、`/app/db` 含 `lead-pool-config` 引用 | 容器内 `grep -rl 'lead-pool-config' /app/src /app/db` → **0 引用** | ❌ 未发布实证 |
| 表数量 | 61 | 61 | ✅ 一致（lead-pool 不新增表，仅改代码/配置/种子） |
| skill_registry | 24 行（16/16 seed） | seed 16/16、memory 16/16 | ✅ 一致 |
| `system_overview_sample` | 存在 | 存在（09-11 已建） | ✅ 一致 |
| 扩展 | age 1.6.0 + vector 0.8.6 | 同 | ✅ 一致 |

**发布判定**：生产落后于本地 9 commit（线索公海池租户隔离特性）——与 09-11 22:00、09-12 一致，期间无 release。该特性跨 `src/db/docs/skills` 多目录，**只能走 `release`**（hotfix 白名单不含 `skills`）。

**⚠ 发布清单（先报用户，未执行）**：
- **改动内容**：线索公海池租户隔离特性（公海/私海 S0/S0P 阶段机、池配置、S0P→S1 硬闸、待办视图过滤）。
- **通道**：`release`（整体 rebuild）。
- **影响面**：商机/线索阶段流转、池配置端点、待跟进视图。
- **发布后必做**：① 恢复 HTTPS（deploy.sh 会抹 nginx 443）；② 核 `.env` 15 键（PGPASSWORD≈32/SMTP_PASS≈30/CRM_LLM_SECRET≈43）；③ 核 `skill_registry` 16/16；④ 核 61 表；⑤ 采样表数据面（生产需实跑 `sample-system-overview.mjs`）。

**待用户执行命令（PowerShell）**：
```powershell
$PY  = "C:\Users\wangchuan08\.workbuddy\binaries\python\envs\default\Scripts\python.exe"
$DIR = "D:\system\CRM-ai-native\scripts\tencent-lighthouse-deploy"
$PW  = "$env:TEMP\crm_ssh.pwd"
& $PY "$DIR\deploy-remote.py" release --password-file $PW
& $PY "$DIR\deploy-remote.py" status  --password-file $PW
```
> 标注：**发布未执行 / 需用户执行**（auto-release 未授权）。禁在生产库执行 DELETE。

---

## ⑦ KMD 闭环探针

**命令**：`node scripts/kmd-closure-probe.mjs --json artifacts/kmd-probe-2026-09-13.json`（即 `npm run probe:kmd`）。
**退出码**：**1**（有 FAIL/ERROR，与任务约定一致；前次管道误读为 0 已纠正）。
**JSON**：`artifacts/kmd-probe-2026-09-13.json`（9.7 KB）。

**结构化状态表（D1–D11 + D13 共 12 条；D12/E2E 需 `--e2e --db=test` 本次跳过）**：

| 探针 | 边 | 状态 | 关键指标 |
|---|---|---|---|
| D1 知识向量真伪 | K 构建 | 🔴 FAIL | 真模型向量占 0%（69/69 条为 hash 伪向量）→ 语义检索等于随机 |
| D2 LK 知识层消费者 | ① K→D 供给 | 🟢 PASS | LK 有 4 处消费者（src\context\injector.js:45/50） |
| D3 L1 知识构成 | ① K→D 供给 | 🟢 PASS | L1 已注册知识 15.44%（69/447） |
| D4 outcome 真实性 | ⑤ 结果→D | 🔴 FAIL | 真自动回写 0 条（总计 4，种子 4）→ 业务结果从未回流 |
| D5 校准事件契约 | ⑥ 校准触发 | 🟢 PASS | 交集 1 项（outcome-set） |
| D6 校准补丁积压 | ⑦ 参数生效 | 🔴 FAIL | PENDING 70 条，最老已积压 **8.3 天** → 校准在产出但无人消费 |
| D7 M→K 升格 | ⑧ M→K | 🟢 PASS | 共 1 条先例，1 条由记忆升格 |
| D8 记忆可用性 | ② M→D 供给 | 🟢 PASS | 活跃可注入 145 行、锚定 75 行 |
| D9 噪声源排行(24h) | M 构建 | 🟡 WARN | 24h 688 行，最大源 visit_shortfall 470 行（68.31%） |
| D10 D→K 回写 | ④ D→K | 🟡 WARN | 复盘产出知识 0 条 → 回写路径存在但从未走通 |
| D11 知识投影契约 | K 构建 | 🟡 WARN | 仅 23.19% 知识能被 LK 正确投影（16/69） |
| D12 结果自动回流 | ⑤ 结果→D | ⚪ SKIP | 需 `--e2e --db=test`（避免写生产库） |
| D13 噪声闸门回归 | M 构建 | 🟢 PASS | 三关全过：on(*) 已除、BLOCKED 硬闸生效、近 60 分钟零 trace 噪声 |

**汇总**：🟢6 🔴3(D1,D4,D6) 🟡3(D9,D10,D11) ⚪2 · exit **1**。

**⚠ 需关注（高亮）**：
- **D1 真向量 0%**（verdict：`真模型向量占 0%（69/69 条为 hash 伪向量）→ 语义检索等于随机`）—— 持续性架构缺口，D1 绿之前禁止声称"语义检索已落地"。
- **D4 真回流 0**（verdict：`真自动回写 0 条（总计 4 条，其中种子 4 条）→ 业务结果从未回流`）—— 09-11 已根治接线（补 import + outcome_event_map 幂等播种），但生产镜像滞后未同步；发布后需复测。
- **D6 积压 70 最老 8.3 天**（较 09-12 的 7.3 天**继续恶化**）—— 校准在产出但无人消费，须管理员审批流消费。

### D6 校准补丁积压治理辅助（triage）

**命令**：`node scripts/calibration-triage.mjs --json artifacts/calibration-triage-2026-09-13.json`（即 `npm run triage:calibration`）· exit 0 · JSON `artifacts/calibration-triage-2026-09-13.json`（36.8 KB）。

**分级待办清单**（total=70，与 09-11/09-12 持平，积压未消项）：
- **HIGH = 10**（本夜实测确认）：如 `f45d379d`（LOSS_REVIEW source_refresh→realtime-pull）、`32856bd8`（QUOTE_PRICING required_dims）、`72576ee9`（LEAD_FOLLOW_UP edge_binding E1）、`0107dfaa`（QUOTE_PRICING dimensions:L1-L7 定时刷新）等。
- **MEDIUM = 32 / LOW = 28**（沿用稳定模式，积压总量未变）。
- 典型 knob/target：`required_dims`→各 scenario、`source_refresh`→各维度源、`edge_binding`→E1、`threshold`→QUOTE_PRICING 0.8、`config_store`→approval-config.reviewThreshold。

> ⚠ **HITL 铁律提示**：校准补丁按治理流程**须经管理员审批流落地**（POST `/api/calibration/patches/<patch_id>/approve` 或 `/api/my-todo/tune-approve`）。**本清单仅用于人工审阅消项，绝不自动应用**。

---

## ⑧ 系统概览采样

**命令**：`node scripts/sample-system-overview.mjs`（经 src/db.js 默认连生产库 crm_native @ localhost:5433，与 D7 同源）。
**stdout**：`[sample-overview] upserted 11 tenants` · **exit 0** · **成功**。

| 项 | 值 |
|---|---|
| 采样租户数 | 11 |
| 三指标写入行数 | k_method_skill / m_precedent_edge / d_l1_intercept 各按租户+system 模板 upsert（复合 PK (tenant_id, sample_date, metric) 幂等覆盖当日值） |
| 成功/失败 | 成功（exit 0） |

- 前置依赖 `crm.system_overview_sample` 表存在（09-11 已发布建出）→ 本次未报错。
- 幂等性：同 (tenant_id, sample_date, metric) 重复运行仅覆盖当日值，不新增历史行；仅写采样表，零业务表副作用。
- 该采样仅服务 `/system-overview/k|m|d` 30 日趋势 SVG，不参与任何业务读写路径。

---

## 待用户执行事项（汇总）

1. **GitHub 推送**（③）：`git push origin feat-multi-industry-meta-model`（沙箱无凭据）。
2. **生产发布**（⑥）：线索公海池特性落后 9 commit，待授权后执行 `release`（PowerShell 命令见 §6）。
3. **D6 校准积压**（⑦）：70 条待消项（HIGH 10 / MED 32 / LOW 28），须管理员审批流逐一落地，**禁自动应用**。
4. **24 个 `D` 专利/IP 文档删除**（②持有）：需用户确认后方可提交。
5. **持续性架构缺口**（⑦ D1 真向量 / D4 真回流）：D4 接线已修但生产镜像滞后，发布后须复测；D1 待接入真 embedding。

## 本夜执行产物
- 报告：`reports/nightly/20260913-audit.md`
- 探针 JSON：`artifacts/kmd-probe-2026-09-13.json`
- 校准 triage JSON：`artifacts/calibration-triage-2026-09-13.json`
