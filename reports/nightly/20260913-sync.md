# 生产系统同步完成报告（2026-09-13 22:22→22:35）

**触发**：用户指令「完成生产系统同步」（对生产发布的显式授权）。
**结论先行**：生产发布已成功执行并验证，线索公海池（9 commit 缺口）已闭合；HTTPS 已恢复；GitHub push 因沙箱无凭据仍待用户；本地工作树已还原。

## 1. 发布执行

| 项 | 结果 |
|---|---|
| 通道 | `deploy-remote.py release`（本机打包→SSH→重建镜像，不走 git） |
| 退出码 | `RELEASE_EXIT=0` |
| 真实性判据 | 表数量 **61**（与发布前一致 → 非假发布、`.env` 未被抹成模板，KEEP_RELATIVE_PATHS 修复生效）；无「下一步：编辑该文件」提示 |
| 扩展 | `age 1.6.0` + `vector 0.8.6` + `pgcrypto 1.3` |
| skills | `[skill-registry] memory applied=16/16`（出厂声明源完整） |
| 关键播种 | `[migrate] 线索池三池模板基线已播种（lead-pool-config）` |

## 2. 功能落地复核（线索公海池）

- 发布前容器 `grep -rl 'lead-pool-config' /app/src /app/db | wc -l` = **0**（缺口实证）
- 发布后 = **5** → **9 commit 线索公海池已真实上线生产**
- 发布日志直接证明：`[migrate] 线索池三池模板基线已播种（lead-pool-config）`

## 3. 发布后健康检查（status）

三容器 healthy、本机 `http://127.0.0.1:3000/` 200、公网 `http://127.0.0.1:80/` 200、61 表、扩展清单完整。

## 4. HTTPS 恢复（release 会重写 nginx 致 443/SSL 丢失）

- 执行：`sed server_name _ → www.chiyuai.com chiyuai.com` + `nginx -t` + `systemctl reload` + `certbot --nginx`
- 结果：`nginx -t` 通过；证书已部署至 `sites-enabled/crm`；HTTPS 在 www.chiyuai.com / chiyuai.com 启用
- 四条点检：
  - `/mcp` → **401**（auth_basic 未被整体跳转吞掉，MCP 网关完好）✅
  - `https://127.0.0.1/landing.html` → **200**（HTTPS 正常）✅
  - `Host: www.chiyuai.com` 127.0.0.1/landing.html → **301**（域名 vhost 跳 HTTPS）✅
  - `ss -tlnp | grep :443` → **1**（443 监听）✅
  - ⚠ `http://127.0.0.1:80/`（裸 IP 根）返回 **200** 而非预期 301

### ⚠ 裸 IP:80 不跳 HTTPS（既存配置冲突，非本次发布引入）
- 根因：服务器两个 `listen 80 default_server` 冲突——`/etc/nginx/sites-available/default:22`（nginx 自带默认站，被启用，直接反代 Express 返回 200）与 `/etc/nginx/conf.d/ip-default.conf:5`（意图对裸 IP 做 301 跳 HTTPS）。stock `default` 胜出。
- 性质：发布前既存（`sites-available/crm` 本次被重写，未动 `default`/`ip-default.conf`）；生产功能完整，仅裸 IP 未强制 HTTPS。
- 处置：**不擅自改线上默认站**（超出本次授权发布范围、有 destabilize 风险），留待用户决策。建议：移除 `sites-available/default` 的 `default_server` 或禁用该站。

## 5. GitHub 推送（待用户）

- 沙箱代理无法连通 `github.com:443`（`PUSH_EXIT=128`，预期）。
- 用户本地执行：
  ```powershell
  git push origin feat-multi-industry-meta-model
  ```

## 6. 本地工作树处理

- 发布前 `git stash push -u`（净化工作树，避免未提交改动漏进发布包）→ 发布后 `git stash pop`（还原 24 个持有删除 `docs/patents/*` + 未跟踪夜批报告）。
- 当前工作树：24 `D`（专利/IP 文档删除，持有未提交） + 未跟踪 `reports/nightly/20260911.md` `20260912.md` `20260913.md`，与发布前一致。

## 7. 待用户后续

1. `git push origin feat-multi-industry-meta-model`（沙箱无凭据）
2. 决策：是否移除 `sites-available/default` 的 `default_server` 以使裸 IP:80 强制跳 HTTPS
3. 确认并提交 24 个专利/IP 文档删除
4. D6 校准积压 70 条走管理员审批流消项
5. 持续性缺口：D1 真 embedding 接入、D4 发布后复测（接线已于 09-11 修，本次发布已带入生产镜像）
